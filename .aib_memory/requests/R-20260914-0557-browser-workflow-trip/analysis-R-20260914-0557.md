## Overview

Request ID: R-20260914-0557

Request title: Локален browser workflow за trip страници

### Background

The site owner needs to create and edit trip journals without remembering generator arguments, coordinating separate registry edits, or configuring a preview server.

### Scope

Extend the existing trip editor with discovery, creation, settings, part management, reviewed Drive generation and preview; add standard-library Python helpers under trips/scripts/, Linux and Windows launchers, template alignment, automated checks and documentation.

### Out of scope

No publishing, Git operations, Drive authentication or recursive discovery, production backend, existing China/2026 content changes, or gallery redesign. Shared gallery changes are permitted only for a demonstrated single-part defect.

## Input Interpretation

### User Original Inputs

The following is the original developer input, preserved verbatim:

### Goal

Да се създаде лесен локален browser workflow за създаване и редактиране на trip страници, без запомняне на аргументите на `scripts/generate_trip_event.py` и без ръчно конфигуриране на локален preview server.

### User and value

- Основен потребител: собственикът на сайта.
- Текущ проблем: създаването на trip/event изисква ръчни команди, редактиране на няколко JavaScript файла и отделно стартиране на локален server.
- Очаквана полза: по-малко ръчни стъпки, по-малък риск от несъответствие между `events.js` и `event-*.js`, и лесен локален preview.
- Други засегнати страни няма; посетителите виждат само публикувания статичен резултат.

### Terminology

- Trip е цял пътепис като `trips/china/`, достъпен през един `index.html`.
- Part е отделна навигируема страница в trip-а, представена от един `data/event-*.js` файл и един запис в `data/events.js`.
- Един trip може да бъде празен, да съдържа само една part или да съдържа множество parts.

### Selected approach

Да се разшири съществуващият `trips/data_editor.html` като единен browser interface за:

- създаване на нов trip;
- избор и редактиране на съществуващ trip;
- редактиране на `trip-config.js`;
- управление на parts чрез `data/events.js`;
- генериране на `event-*.js` от публични Google Drive папки;
- локален preview на trip-а и избраната part.

Editor-ът да се стартира с двойно щракване чрез Linux `.desktop` и Windows `.bat` launcher. Launcher-ите да стартират ограничен локален Python процес, да обслужват repository root през HTTP и да отварят editor-а в браузър.

### New trip workflow

- Показване на действие „Create new trip“.
- Задължително поле за име на trip-а.
- Автоматично генериране на редактиируем URL-safe slug.
- Създаване само под `trips/<trip-slug>/`.
- Копиране на структурата и файловете от `trips/template/`.
- Блокиране на операцията, ако директорията вече съществува, с възможност съществуващият trip да бъде отворен в editor-а.
- Google Drive URL да бъде незадължителен, така че да може да се създаде празен trip.
- Българският език и locale да бъдат defaults; английският да бъде наличен като избор.
- Кратката форма да съдържа основните полета, а останалите настройки на `trip-config.js` да бъдат в разгъваем advanced panel.
- След създаване новият trip автоматично да бъде избран в editor-а.

### Trip settings

Да се добави панел „Trip settings“, който зарежда и записва поддържаните стойности от избрания `trip-config.js`, включително:

- language и locale;
- page title;
- hero eyebrow;
- heading;
- subtitle;
- trip facts;
- page description;
- поддържаните theme стойности;
- локализираните labels.

Записването да запазва валиден `window.GALLERY_TRIP_CONFIG` формат и да не премахва поддържани стойности без предупреждение.

### Trip selection

- При стартиране през launcher editor-ът автоматично да открива валидните директории под `trips/<slug>/`.
- Template и shared директориите да не се показват като реални trips.
- Да има dropdown за избор на trip.
- Да се запази резервната възможност за ръчно избиране на `data/` папка чрез текущия File System Access workflow.
- Съществуващите event editing, rich-text, media visibility, rotation, ordering и AI функции да останат работещи.

### Part and events.js workflow

- Съществуващият registry editor за `events.js` да бъде използван и подобрен.
- Потребителят да може да добавя, редактира, подрежда и изтрива parts.
- За нова part да се въвеждат дата, заглавие и публичен Google Drive folder URL.
- Slug-ът и името `event-*.js` да се генерират автоматично от датата и заглавието, но да могат да бъдат редактирани преди запис.
- Google Drive URL да се използва за генерирането, без да се добавя към публичната схема на `events.js`.
- Един Drive URL да генерира една part от директните медийни файлове в папката.
- Добавянето на следващи parts да става през същия registry editor.
- Trip с една part да бъде напълно поддържан без изискване за допълнителни registry записи.

### Drive generation

- Да се използва съществуващата логика от `scripts/generate_trip_event.py`, без дублиране на parser и validation поведението, но за целта да се генерират нови скриптове в `trips/scripts/` папка.
- Да се запазят текущите ограничения: публична папка, без credentials, само директни файлове, без обхождане на подпапки.
- Преди запис да се покажат резултатът от откриването, броят валидни медии и пропуснатите елементи.
- След потвърждение `event-*.js` и съответният запис в `events.js` да се запишат като една координирана операция.
- При неуспешно генериране `events.js` да остане непроменен.
- При неуспешен registry запис да не остава недостъпен частично създаден event.
- Повторно генериране на съществуващ файл да бъде блокирано по подразбиране.
- Замяна да се допуска само след явно предупреждение, че описания, text blocks, visibility, corrections и custom ordering ще бъдат загубени.

### Preview

- Launcher-ът да стартира local HTTP server, ограничен до loopback интерфейса.
- Editor-ът да има бутон „Preview trip“.
- При избрана part preview бутонът да отваря URL с правилния `#event=<slug>` hash в нов browser tab.
- Preview да работи без ръчно въвеждане на server команда или URL.
- Спирането на launcher процеса да прекратява local server-а чисто.

### Template alignment

- `trips/template/index.html` да използва `../shared/gallery.css` и `../shared/gallery.js`, съответстващи на структурата `trips/<slug>/` и на `trips/china/index.html`.
- Съществуващите `trips/china/index.html` и `trips/2026/index.html` да не се променят в рамките на тази заявка.
- Новите trips да се създават единствено от актуализирания template.

### Constraints

- Публикуваните trips трябва да останат чисти статични HTML, CSS и JavaScript файлове за GitHub Pages.
- Local helper може да използва Python 3.10+, но без външни Python packages, package manager или build system.
- Local helper трябва да слуша само на loopback адрес и да ограничава file operations до repository-то и `trips/`.
- Съществуващата схема `window.GALLERY_EVENT_INDEX` с `slug`, `title` и `data` трябва да се запази.
- Съществуващата схема `window.GALLERY_ITEMS` и поведението на shared gallery трябва да се запазят.
- Съществуващият CLI generator и тестовете му трябва да продължат да работят.
- Работата трябва да е съвместима с Linux и Windows launchers.

### Out of scope

- Git commit, push или автоматично публикуване в GitHub Pages.
- Google Drive authentication, OAuth или промяна на Drive permissions.
- Рекурсивно обработване на Drive подпапки.
- Промяна на съдържанието на съществуващите China и 2026 trips.
- Production server или server-side dependency за публикувания сайт.
- Промяна на shared gallery дизайна, освен ако е необходима минимална корекция за коректно показване на trip с една part.

### Assumptions

- Python 3.10+ е наличен на Linux и Windows машините. Ако не е, launcher-ът няма да може да стартира local helper-а.
- Drive папките са публично достъпни. Ако не са, съществуващият generator трябва да върне ясно съобщение за грешка.
- Един Drive folder съответства на една navigable part.
- Drive URL не е необходимо да се съхранява в `events.js`; ако по-късно е нужно повторно генериране, URL се въвежда отново.
- Chromium остава необходим за резервния File System Access режим; основният launcher workflow може да използва local helper API.

### Acceptance criteria

- Двойно щракване върху Linux или Windows launcher стартира editor-а и local server без ръчно въвеждане на аргументи.
- Editor-ът показва валидните trips под `trips/` и позволява резервен ръчен избор на `data/` папка.
- Нов празен trip може да бъде създаден от template-а само с име.
- Опит за създаване върху съществуващ slug не променя нито един файл.
- `trip-config.js` може да бъде създаден и впоследствие редактиран чрез „Trip settings“.
- Една публична Drive папка може да създаде една part, нейния `event-*.js` файл и синхронизиран запис в `events.js`.
- Към съществуващ trip могат да се добавят множество parts.
- Trip с точно една part се зарежда и preview-ва коректно.
- Неуспешна Drive validation не променя `events.js`.
- Замяна на съществуващ event файл изисква явно предупреждение и потвърждение.
- „Preview“ отваря правилния local HTTP URL и избраната `#event=<slug>` страница.
- Всички текущи editor функции и CLI generator тестове остават работещи.
- Добавят се автоматизирани тестове за path validation, trip creation, collision protection, config serialization, registry synchronization и failure rollback.
- `trips/README.md` се актуализира към каноничната структура `trips/<slug>/`, новите launchers и editor workflow.
- `.aib_memory/context.md` се актуализира с текущата `trips/` архитектура и новия локален workflow.


### Answered follow-up input

- Q001 — Part deletion: Delete both. Delete registration and its data file after explicit confirmation, with coordinated runtime rollback.
- Q002 — Published part identity: Keep saved identity. Keep saved slug and filename immutable; title, content and order remain editable.
- Q003 — Cyrillic slugs: Latin transliteration. Generate ASCII slugs using a documented Bulgarian-to-Latin mapping and validate edited slugs.
- Q004 — Config compatibility: Preserve literals, block executable syntax. Preserve known and unknown JSON-compatible literal fields; reject executable syntax without writing.
- Q005 — Linux launcher setup: Repository-local launcher. Keep a relocatable .desktop entry in the repository and derive paths from its own location.
- Q006 — Legacy registries: Repair before registry save. Permit reading and existing event editing, but require all registry errors repaired before any registry-changing operation.
- Q007 — Interrupted commits: Runtime rollback. Restore originals on caught write failures; abrupt termination may require manual recovery, without a restart journal.
- Q008 — Unsaved preview: Save after confirmation. Offer to save all affected valid drafts before preview; cancellation or any save failure prevents opening preview.

### AI interpretation

The owner will launch a local editor backed by a Python 3.10+ loopback service that serves repository files and performs constrained trip operations. Published output remains static. A trip owns index.html, trip-config.js and data/; a part owns a registry entry and event data file. Empty trips and a single part are valid states.

Creation requires only a name, derives an editable slug, copies the corrected template exclusively into trips/<slug>/ and rejects existing destinations without modification. Bulgarian/bg-BG is the initial language/locale, with English available. Basic identity inputs precede advanced theme and label settings. Settings preserve every supported value and valid window.GALLERY_TRIP_CONFIG serialization. Complete BG and EN label presets in newly generated configs avoid relying on mixed shared defaults.

The launcher mode discovers valid trips and supports all current event, registry, rich text, media corrections, ordering, visibility and AI workflows through a storage adapter. Manual data-folder access remains a separate fallback; a data-folder handle alone does not grant access to its parent config. Helper-only controls must explain their availability in fallback mode.

Generation reuses existing discovery, selection, public-access validation and serialization functions. New orchestration belongs in trips/scripts/ and imports scripts/generate_trip_event.py rather than duplicating its parser. Discovery is read-only, exposes skipped items and valid counts, and retains a prepared result for explicit confirmation. Commit validates current files and collision conditions again, then coordinates event and registry changes, rolling back on failure. Replacement requires the full warning including lost corrections. The Drive folder URL is never added to the public registry.

Registry selection must use stable identity rather than a mutable row index. Dirty settings, registry and event drafts require independent guards. Late polling or AI results cannot update a newly selected trip. Preview targets the helper origin and trips/<slug>/index.html with the selected encoded event hash. Trip creation with an optional Drive URL is interpreted as creating the empty trip first, then offering the normal reviewed generation operation; a Drive failure leaves the explicitly created empty trip usable.

The eight answered Q-blocks resolve deletion, identity, transliteration, literal config preservation, repository-local Linux launch, legacy validation, runtime rollback and save-before-preview as recorded above. Deletion must reject a still-referenced data target and retain recoverable originals during the operation. Runtime rollback covers coordinated preview saves as well as generation and deletion; restore failures must identify recovery files and block further writes. A Linux launcher must handle the desktop location as a local filename or file URI, and fail clearly when unavailable; no generated installer is included.

The requested work is treated as the current implementation priority, with no external deadline or additional stakeholders. The full scope is one integrated authoring workflow, decomposed into bounded tasks below. Linux desktop environment trust requirements and Python installation are external prerequisites; their failure must produce actionable guidance.

## Research Results

### Workspace pattern-scan

- trips/data_editor.html is a roughly 3,000-line self-contained editor. loadEventsJs, loadEvent, saveEventsJs, handleSave and refreshSelectedEvent depend directly on File System Access handles. A helper transport must cover all reads, writes, restoration and polling, not just the new creation form.
- The registry currently permits free slug/title/data edits, adds blank rows and removes registrations without deleting files. currentEventIndex can lose correspondence to the loaded handle after reorder or deletion; retain selected data identity and invalidate removed selections.
- scripts/generate_trip_event.py exposes discover_folder_items, select_media_items, validate_selected_media and serialize_event. Capture skip diagnostics with an in-memory text stream. Do not call run_generation during discovery: it writes output and handles CLI consent.
- The generator validates selected media sequentially with a 30-second per-request default; total duration can scale with folder size. The service must keep static preview responsive, prevent duplicate commits, surface progress/busy state and discard stale results.
- Generator output uses atomic replacement for one file, explicitly refuses events.js and never coordinates registry updates. The helper therefore owns the two-file operation and recovery policy.
- tests/test_generate_trip_event.py uses unittest, synthetic fixtures and importlib loading from the existing scripts path. Retain that path and existing callable behavior; add helper coverage without external packages or network access.
- trips/template/index.html uses /trips/shared/ paths; trips/china/index.html already uses ../shared/ paths. trips/2026/index.html uses root-relative paths but is explicitly excluded from edits. The template registry is empty; event-template.js is an unregistered placeholder.
- trips/shared/gallery.js initializes whenever normalized events are nonempty and falls back to events[0]; there is no observed two-event minimum. A single-part fix is conditional on a failing behavioral test, not assumed necessary.
- Gallery configuration supports 13 semantic theme keys, including heroWash. BG labels in trips/china/trip-config.js provide a reusable reference; existing trip files must only be read. Localization must include shell label IDs as well as dynamically generated state messages.
- No context References section or non-placeholder attachments were present. Persistent instructions.md is empty. The existing analysis was backed up to the active request folder before applying all eight answered Q-blocks. No closed request or archived input was read.

### Industry knowledge

- Python documents that SimpleHTTPRequestHandler follows symbolic links. Applicability: constrain resolved static paths to the repository, reject private/internal paths and traversal, and constrain writes further to allowed trip files; simply choosing a serving directory is insufficient. Source: [Python HTTP server documentation](https://docs.python.org/3/library/http.server.html).
- Python documents os.replace as a single-path replacement operation, with possible cross-filesystem failures. Applicability: stage in the destination filesystem; two replacements do not provide a transaction over event and registry. Use serialized commits, version checks, retained originals and rollback; persistent crash recovery is a separate decision. Source: [Python 3.10 os documentation](https://docs.python.org/3.10/library/os.html#os.replace).
- The Desktop Entry specification defines Exec parsing, quoting and working-directory fields rather than shell expansion. Applicability: a relative script path cannot safely depend on the file manager working directory; use the user-selected relocatable entry bootstrap with the standalone %k argument, decode a local file URI when supplied, and test the desktop environment. An empty or unavailable location must produce an actionable error rather than guess a working directory. Source: [Desktop Entry specification](https://specifications.freedesktop.org/desktop-entry/latest-single/).

These findings guide engineering safeguards, not autonomous resolution of product choices. No live Drive request was made because no folder URL was supplied; the existing undocumented HTML dependency remains an integration risk.

### AI Agent critique

- .aib_memory/input.md now answers all eight implementation choices; no unanswered product decision remains. The retained minimum_questions value is not a reason to repeat resolved questions.
- .aib_memory/context.md describes 202507_china/ and an app.js dropdown architecture; current trips use trips/<slug>/ and shared/gallery.js with date-grouped navigation.
- .aib_memory/context.md states an unqualified prohibition on server-side code/dependencies, conflicting with the expressly requested local authoring helper; retain the prohibition for deployed pages while adding the local-only exception.
- trips/README.md documents root-level trips, root-level copy destinations, manual preview and no editor generation; those instructions conflict with the requested canonical workflow.
- trips/template/index.html has root-relative shared paths and a comment describing them; both need alignment. Its English static shell must agree with generated language settings.
- trips/shared/gallery.js mixes Bulgarian and English default labels and includes the typo “Журал”; avoid altering shared presentation by writing complete new-trip label presets.
- trips/shared/gallery.js presents the empty registry as an error/unavailable trip. New empty-trip configs can provide neutral wording, but changing shared empty-state styling exceeds the stated gallery exception.
- trips/2026/trip-config.js uses English language/locale with Bulgarian identity copy. This existing inconsistency is excluded from edits.
- trips/data_editor.html evaluates trusted source with new Function; shadowing window is not a security sandbox. The helper must never evaluate arbitrary JavaScript in Python or treat browser parsing as server-side path validation.
- trips/data_editor.html uses mutable numeric event selection while registry rows can move or disappear, risking selection/handle mismatch.
- trips/data_editor.html does not validate registry uniqueness or referenced file existence before saving and does not separately track registry dirty state.
- scripts/generate_trip_event.py depends on undocumented Drive HTML and validates access through a thumbnail probe; success cannot guarantee later full-resolution/video availability.
- scripts/generate_trip_event.py has preview-oriented validation docstrings despite thumbnail probing, and its replacement warning omits media corrections that the new UI must explicitly mention.
- scripts/generate_trip_event.py supports more video extensions than the shared viewer classifies as video; preserve existing schemas and record this pre-existing mismatch without broadening gallery changes.
- tests/test_generate_trip_event.py covers generator behavior but does not demonstrate helper path safety, registry transactions or browser preservation; those tests are new deliverables.
- .aib_brain/prompts/aib-analyze.md duplicates variables, skips S05.7, names two subsections while demanding three, conflicts on reset timing and plan Decisions headings, and asks for archival input use despite prohibiting archive reads. Use the named two-subsection schema, numbered reset-before-questions sequence and retained original input; record the contradictions rather than modify tooling.
- .aib_brain/conventions/analysis-convention.md repeats the impossible three-subsection count and requires neutral decision presentation, whereas q-block-convention.md requires one recommendation. Keep alternatives neutral in the Decision Register and mark one recommendation only in Q-blocks.
- .aib_brain/conventions/plan-convention.md simultaneously requires exact commands and prohibits implementation snippets. Future plan generation must use its four-section schema and the explicitly permitted literal context-edit commands.
- .aib_brain/tools/verify-input.py and verify-context.py persist verification results, conflicting with the prompt's unchanged-workspace requirement on failure; both passed this run.
- .aib_brain/tools/finalize-input.py describes an atomic archive/move/reset sequence but implements sequential filesystem operations without rollback; failure during the sequence can leave partial lifecycle changes.

- trips/start-editor.desktop is a planned artifact: %k may be a filename, URI or empty, so portability and OS trust cannot be established by quoting inspection alone; verify an actual Linux desktop launch.
- Runtime-only rollback cannot guarantee recovery after abrupt termination or after a failed restoration; retained originals and actionable recovery errors are required, without claiming crash atomicity.

No additional concrete issue was identified in the read instructions, empty registry template, log-entry script, input-header.py, edit-context.py or requirements gate convention beyond the findings above; prior China findings are retained from the existing analysis. This is a scoped review of inspected code, not an exhaustive audit of all repository files.

### Edge Cases

- Empty names; Cyrillic-only names; colliding generated slugs; Windows reserved names, trailing dots/spaces and case-only collisions; encoded traversal, drive-letter paths, UNC paths and symlink escapes.
- Absent, unreadable or malformed trip/config/registry files; template/shared/scripts excluded from discovery; an existing destination or symlink must remain untouched.
- Empty registry, single part, stale hash, missing event file, duplicate slug or duplicate data target; existing legacy records must not be silently normalized.
- Unknown config keys, nested values, comments, computed JavaScript, quotes, newlines and Unicode separators; language changes must not erase custom labels or theme overrides.
- Unsupported Drive items, nested folders, private children, zero supported media, timeout, changed discovery result, cancelled confirmation and expired prepared operation.
- Existing generated target, denied overwrite, changed event/registry since discovery, concurrent browser tabs, file permission failures and Windows file locks.
- First event write succeeds and registry write fails; restoring overwritten originals fails; interruption between writes; retry must not duplicate the registry entry.
- Switching trips with dirty drafts or an active AI request; polling completes after a switch; registry reorder/removal while editing; helper unavailable while fallback remains usable.
- Port already occupied, repeated launch, repository paths with spaces/non-ASCII, browser launch failure, missing Python and clean process shutdown.
- Preview with unsaved changes; origin changes affect stored settings/handles; foreground polling must retain last-valid state and prevent overlapping reads.

### Requirements Gate Evaluation

Requirements Gate: 8/8 PASS — all categories satisfied.

All checklist items were assessed: one user outcome and explicit need; owner role and no other impacted parties; concrete benefit and current-request priority assumption; bounded integrated scope with explicit technical constraints; specific exclusions; stated Python/public-Drive/browser prerequisites and failure implications; observable acceptance criteria covering every deliverable; and context conflicts resolved by the local-authoring exception and verified current paths. All eight implementation decision points below are now resolved by the answered Q-blocks. Acceptance additionally includes neutral empty-state copy, safe draft switching, preserved language overrides, launcher shutdown and the selected recovery policy.

## Proposed Solution

### High-Level Concept

Extend the existing editor with a standard-library local service and a shared storage interface so the owner can create, configure, populate and preview static trips from one browser workflow. Reuse the current generator and gallery schemas to preserve published behavior.

### Execution Steps

#### Task 1: Local service and storage operations

- trips/scripts/trip_editor_server.py: Add loopback HTTP serving, strict Host/Origin checks and per-session mutation authorization, constrained static reads and trip-only writes, discovery, settings/event/registry endpoints, bounded requests, stable default port with explicit conflict handling, browser launch and clean shutdown.
- trips/scripts/trip_storage.py: Add cross-platform path and slug validation, exclusive template-based creation, config parsing/serialization, versioned reads and writes, serialized coordinated commits and rollback. Use documented Bulgarian-to-Latin ASCII slug generation. Parse literal assignments without executing source, preserve unknown JSON-compatible values, and report executable syntax without overwriting it. Permit legacy reads/event edits but block registry changes until all registry errors are repaired. Coordinate generation, confirmed deletion of both registry and data, and preview draft saves with retained originals and runtime rollback; do not add a restart journal.
  - Preserve all supported theme/label fields, reject path escapes even when browser checks pass, and scope runtime-created trip artifacts under trips/<slug>/.


#### Task 2: Reuse Drive discovery and confirmation

- trips/scripts/drive_generation.py: Import the existing generator functions through a repository-relative module loader; return discovery/skip summaries and a prepared generation identifier, then consume it once during a version-checked coordinated commit. Do not duplicate parsing or validation logic.
  - Reject zero valid media and all public-access failures; collision checks and explicit overwrite consent include descriptions, text blocks, visibility, corrections and ordering. Do not place folder URLs in public output.

#### Task 3: Extend the existing browser editor

- trips/data_editor.html: Introduce helper and File System Access storage adapters; add trip dropdown, creation form, basic/advanced settings and complete BG/EN presets, reviewed Drive generation, enhanced registry actions and preview. Preserve item UI, rich text, visibility, corrections, AI controls, session restoration and guarded polling; track trip/config/registry/event drafts and stable selection identities. Lock saved slugs and filenames, allow registry repair, confirm removal of both registration and event file, and offer confirmed saving of all valid affected drafts before preview; cancel preview on cancellation or save failure.


#### Task 4: Align template and launchers

- trips/template/index.html: Use ../shared/gallery.css and ../shared/gallery.js and align explanatory comments; preserve the existing accessible gallery shell.
- trips/template/trip-config.js: Supply Bulgarian defaults and neutral empty-trip copy, with complete supported settings retained; creation may replace identity and presets for the chosen language.
- trips/start-editor.bat: Locate the repository via launcher location, select available Python 3.10+, invoke the helper with quoted paths and retain actionable startup errors.
- trips/start-editor.desktop: Add a repository-local relocatable Linux entry with correct Exec quoting, standalone %k location forwarding and a terminal-owned helper lifecycle; bootstrap the helper from the local filename or file URI and diagnose unavailable location/Python. Do not add an installer.


#### Task 5: Verification

- tests/test_trip_editor.py: Add network-free unittest coverage for path validation, creation, collisions, config round trips, registry validation, prepared generation, consent, version conflicts, rollback including coordinated deletion and preview save, runtime restoration failures, and launcher path decoding using temporary repositories and injected failures.
- tests/trip_editor_browser.html: Add a dependency-free browser test harness for storage adapters, draft guards, registry selection, serialization and zero/one/multiple-part preview; use temporary fixtures without modifying actual trips.
- python -B -m unittest discover -s tests -p 'test_generate_trip_event.py' -v: Expect the existing generator suite to pass with no network dependency.
- python -B -m unittest discover -s tests -p 'test_trip_editor.py' -v: Expect helper tests to pass with fault injection and originals preserved on rejected operations.
- trips/shared/gallery.js: Make a minimal single-part correction only if the browser test demonstrates a defect; otherwise make no edit.
  - Browser/manual acceptance also covers real Linux and Windows launch/stop behavior, helper-mode Chrome/Firefox/Edge, Chromium fallback, preview hashes, rich-text/media/AI preservation, and one supplied public Drive folder. Do not claim these checks from static inspection.

#### Task 6: Documentation and context

- trips/README.md: Replace root-level trip instructions with trips/<slug>/, launcher prerequisites, editor modes, settings, reviewed generation, replacement loss warning, confirmed deletion of data and registry, locked saved identities, literal preservation, full-registry repair, runtime-only recovery and confirmed draft saving before preview and automated/manual verification instructions.
- .aib_memory/context.md: Apply the intent-preserving architecture and local-only requirement changes below; update File Structure and register the critique findings in Issues with scope labels instead of silently fixing excluded code.

## Context Update Analysis

### Context Elements to Add

Add the trip/part distinction and trips/<slug>/ ownership model; Python 3.10+ standard-library local authoring with launchers; helper and manual-folder modes; BG/EN settings and preserved overrides; reviewed Drive generation with coordinated writes and consent; loopback/path constraints; static preview hashes; the eight resolved acceptance rules, including runtime-only recovery, confirmed deletion of both artifacts, locked saved identities and confirmed draft saving before preview. File Structure must include shared/, template/, data_editor.html and the final helper, launcher and test paths.

### Context Elements to Modify/Remove

Replace the obsolete Solution statement beginning “China trip gallery (202507_china/) uses IIFE-scoped app.js” with the verified trips/china/ and shared/gallery.js architecture. Replace the standalone editor statement naming 202507_china/data/ with current trips/data_editor.html and dual storage modes. Update the app.js dropdown description to grouped shared navigation. Scope the existing selected-file polling/visibility statements to shared gallery/editor paths without weakening their semantics. Replace the stale 202507_china/ File Structure subtree with the actual trips layout; retain auxiliary comments tools under trips/china/.

The exact Requirements statements needing replacement are:

- “All pages and features must operate as static files with no server-side code.”
- “No server-side dependencies or build tools may be introduced.”

Replace their meaning with: all published pages operate as static files without a production server; no published server dependencies or build tools are introduced; the local authoring helper may use Python 3.10+ standard library only. Update the app.js rich-text requirement to name trips/shared/gallery.js while retaining trusted HTML body/desc rendering. Keep GALLERY_EVENT_INDEX and GALLERY_ITEMS schemas unchanged.

### Conflict Resolution & Intent Preservation

The developer explicitly authorizes a local helper and explicitly prohibits a production backend; narrowing the two global prohibitions to deployed features preserves their hosting intent. Canonical paths change, not content ownership or supported event/media schemas. Existing portfolio Product/Requirements/Solution entries remain unaffected. The editor's offline claim applies to manual local editing; Drive discovery and existing AI actions require their existing network services. Record stale out-of-scope issues as observations, without correcting China/2026 content or widening shared gallery work.

At plan generation, re-read exact current context statements and embed literal edit-context.py operations per the plan convention. Do not change context before implementation. The three-subsection contradiction is resolved here by using the two explicitly named Proposed Solution subsections, consistent with their defined order and scope.

## Decision Register

### Decision Points

#### Decision Point: Part deletion

- Tag: resolved-by-user
- Applies to: Task 3 registry removal.
- Alternative — Delete both: Remove the registration and its data file with explicit consent and coordinated recovery; avoids orphan files but discards authored content; adds delete transaction handling.
- Resolution: .aib_memory/input.md Q001 explicitly selected Delete both. Delete registration and its data file after explicit confirmation, with coordinated runtime rollback.

#### Decision Point: Published part identity

- Tag: resolved-by-user
- Applies to: Task 3 registry identity editing.
- Alternative — Keep saved identity: Allow title/content/order edits but keep saved slug and filename stable; preserves bookmarks but requires a new part for a new identity; adds locked identity fields.
- Resolution: .aib_memory/input.md Q002 explicitly selected Keep saved identity. Keep saved slug and filename immutable; title, content and order remain editable.

#### Decision Point: Cyrillic slugs

- Tag: resolved-by-user
- Applies to: Task 1 slug generation.
- Alternative — Latin transliteration: Use a documented Bulgarian-to-Latin mapping and ASCII slug validation; produces readable portable URLs but loses original spelling; adds a mapping shared by generation and validation.
- Resolution: .aib_memory/input.md Q003 explicitly selected Latin transliteration. Generate ASCII slugs using a documented Bulgarian-to-Latin mapping and validate edited slugs.

#### Decision Point: Config compatibility

- Tag: resolved-by-user
- Applies to: Tasks 1 and 3 config serialization.
- Alternative — Preserve literals, block executable syntax: Round-trip supported and unknown JSON-compatible literal fields and refuse unsupported executable syntax; prevents silent loss but some hand-authored configs need manual editing; adds a constrained literal parser.
- Resolution: .aib_memory/input.md Q004 explicitly selected Preserve literals, block executable syntax. Preserve known and unknown JSON-compatible literal fields; reject executable syntax without writing.

#### Decision Point: Linux launcher setup

- Tag: resolved-by-user
- Applies to: Task 4 desktop launch.
- Alternative — Repository-local launcher: Keep the .desktop entry in the repo and bootstrap from its own location; avoids generated installation but must be verified against the intended file manager; uses a relocatable entry.
- Resolution: .aib_memory/input.md Q005 explicitly selected Repository-local launcher. Keep a relocatable .desktop entry in the repository and derive paths from its own location.

#### Decision Point: Legacy registries

- Tag: resolved-by-user
- Applies to: Tasks 1 and 3 validation.
- Alternative — Repair before registry save: Allow reading and existing event editing, but require all registry validation errors to be corrected before a registry-changing operation; stronger consistency but unrelated old errors can block generation; adds repair feedback.
- Resolution: .aib_memory/input.md Q006 explicitly selected Repair before registry save. Permit reading and existing event editing, but require all registry errors repaired before any registry-changing operation.

#### Decision Point: Interrupted commits

- Tag: resolved-by-user
- Applies to: Task 1 coordinated persistence.
- Alternative — Runtime rollback: Roll back caught write failures while the helper runs and document abrupt-termination limitations; smaller implementation but manual recovery may be needed after a crash; adds runtime fault tests only.
- Resolution: .aib_memory/input.md Q007 explicitly selected Runtime rollback. Restore originals on caught write failures; abrupt termination may require manual recovery, without a restart journal.

#### Decision Point: Unsaved preview

- Tag: resolved-by-user
- Applies to: Task 3 preview action.
- Alternative — Save after confirmation: Offer to save all affected valid drafts before preview and cancel preview if a save fails; preview includes intended changes but triggers writes after consent; adds coordinated save flow.
- Resolution: .aib_memory/input.md Q008 explicitly selected Save after confirmation. Offer to save all affected valid drafts before preview; cancellation or any save failure prevents opening preview.
