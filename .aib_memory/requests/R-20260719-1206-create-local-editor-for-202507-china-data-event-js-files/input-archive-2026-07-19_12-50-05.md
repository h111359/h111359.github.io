---
state:
  request_id: R-20260719-1206
  title: Create local editor for 202507_china/data event JS files
  status: questions_generated
  input_verification_result: valid
  context_verification_result: valid
options:
  minimum_questions: 25
  input_verification_enabled: true
  context_verification_enabled: true
---

## Input

## Questions

**Q001**: Which file access mechanism should the editor use to load and save event JS files from the local filesystem?
> **Why this matters:** This determines whether the editor requires manual upload/download per file or can read/write files directly in place, significantly affecting the editing workflow.
- [ ] Option A: File API — `<input type="file">` for upload and `Blob` download via anchor; works in all browsers including Firefox *(recommended)*
- [x] Option B: File System Access API — `showDirectoryPicker()` for direct folder access; read/write in place; Chromium-based browsers only (Chrome, Edge)
- [ ] Option C: Hybrid — attempt File System Access API if available; fall back to File API automatically when not supported
- [ ] Other: ___

**Q002**: How should rich text formatting (bold, colors, etc.) be stored in the `desc` and `body` string fields of event JS files?
> **Why this matters:** The storage format determines how formatting is serialized into JS files and whether the gallery viewer (`app.js`) needs updating to render it.
- [x] Option A: HTML markup stored directly in string fields (e.g., `<b>bold</b>`, `<span style="color:red">text</span>`) *(recommended)*
- [ ] Option B: Markdown syntax (e.g., `**bold**`, custom color tag)
- [ ] Option C: Plain text only — no changes to storage format; no rich formatting support added
- [ ] Option D: Custom lightweight markup (e.g., `[b]bold[/b]`, `[color=red]text[/color]`)
- [ ] Other: ___

**Q003**: Should the gallery viewer (`app.js`) be updated to render formatted text from `desc` and `body` fields in the gallery display?
> **Why this matters:** Currently `app.js` escapes all text via `esc()`, which prevents HTML or Markdown from rendering in the gallery; updating it is a separate change beyond the editor itself.
- [x] Option A: Yes — update `app.js` to render formatted content from `body`/`desc` fields *(recommended)*
- [ ] Option B: No — formatting stored but not rendered in the gallery; only visible in the editor
- [ ] Other: ___

**Q004**: What does "size" mean as an editable attribute in the context of event JS files?
> **Why this matters:** Determines whether a new `size` field needs to be added to the `window.GALLERY_ITEMS` schema and how the gallery layout would need to change.
- [ ] Option A: A display-size flag (e.g., `"large"` / `"medium"` / `"small"`) controlling grid column span in the gallery
- [ ] Option B: Explicit pixel width/height fields (`width`, `height`) for the gallery image display
- [ ] Option C: "Size" refers only to thumbnail preview size within the editor itself — not a data field; no schema change needed *(recommended)*
- [x] Option D: No size field — user does not want to add one
- [ ] Other: ___

**Q005**: Should the editor support editing `events.js` (the event registry file) in addition to per-event data files?
> **Why this matters:** `events.js` is in the `202507_china/data/` folder; adding events.js editing allows managing the full event list (add/remove/reorder events), but increases implementation scope.
- [x] Option A: Yes — include `events.js` editing; allow adding, removing, and reordering event entries; edit slug, title, and data path
- [ ] Option B: No — only per-event data files; `events.js` is used read-only for navigation only *(recommended)*
- [ ] Other: ___

**Q006**: Should the new editor be a new standalone file, or should it replace or extend the existing `comments_index.html` / `comments_app.js`?
> **Why this matters:** Determines whether the existing partial editor (`comments_index.html`) is preserved alongside the new editor or superseded by it.
- [x] Option A: New standalone file — create `202507_china/data_editor.html` alongside the existing files; both coexist *(recommended)*
- [ ] Option B: Replace existing — replace `comments_index.html` and `comments_app.js` entirely with a new implementation
- [ ] Option C: Extend existing — add new capabilities to `comments_index.html` and `comments_app.js` incrementally
- [ ] Other: ___

**Q007**: How should the editor allow the user to load and switch between events?
> **Why this matters:** The navigation model determines how quickly the user can move between the 47+ event files and directly affects the editing workflow.
- [x] Option A: Dropdown — `<select>` dropdown populated from the loaded event registry listing all events by title *(recommended)*
- [ ] Option B: Sidebar file tree — collapsible sidebar listing all events for click-to-load navigation
- [ ] Option C: One file at a time — no event navigation UI; user manually loads each event file individually
- [ ] Other: ___

**Q008**: Should the editor support reordering items within an event?
> **Why this matters:** Item order in `window.GALLERY_ITEMS` determines the display order in the gallery; supporting reordering enables non-destructive restructuring of event layouts.
- [ ] Option A: Drag-and-drop reordering using the HTML Drag and Drop API
- [x] Option B: Move-up / move-down arrow buttons per item *(recommended)*
- [ ] Option C: No reordering support
- [ ] Other: ___

**Q009**: When should the editor write changes back to the file?
> **Why this matters:** The save timing determines whether in-progress edits could be accidentally lost and how often the user needs to interact with save controls.
- [x] Option A: Manual save only — explicit Save / Download button; no automatic writes *(recommended)*
- [ ] Option B: Auto-save to `localStorage` on every change, plus explicit Export to file button
- [ ] Option C: Auto-download triggered after a debounce timeout on every change
- [ ] Other: ___

**Q010**: Should the editor support undo/redo for structural changes (add, delete, reorder)?
> **Why this matters:** Without a custom undo stack, deleting or reordering items is irreversible within the same session (native browser undo only applies to text field typing).
- [x] Option A: Native browser undo (`Ctrl+Z`) within text input fields only — no custom implementation *(recommended)*
- [ ] Option B: Custom undo/redo stack covering all structural changes (add, delete, reorder, and field edits)
- [ ] Option C: No undo support at all
- [ ] Other: ___

**Q011**: How should rich text editing be implemented in the editor? (Applies only if Q002 is not "plain text only")
> **Why this matters:** The implementation approach determines whether external libraries are needed and how the formatting toolbar behaves; directly affects offline usability.
- [x] Option A: `contentEditable` div with a custom formatting toolbar using `document.execCommand()` — no external library, fully inline *(recommended)*
- [ ] Option B: Plain `<textarea>` with Markdown/custom syntax input and a live preview div rendered by a small inline parser — no external library
- [ ] Option C: Load a lightweight rich text library from CDN (e.g., Quill.js, TipTap) at runtime
- [ ] Other: ___

**Q012**: Which set of formatting options should be supported in the rich text toolbar? (Applies only if Q002 is not "plain text only")
> **Why this matters:** The scope of formatting controls determines implementation size, the markup stored in data files, and the rendering requirements in the gallery viewer.
- [ ] Option A: Minimal — bold and foreground color only, plus preserved newlines
- [ ] Option B: Standard — bold, italic, foreground color, and underline *(recommended)*
- [x] Option C: Extended — bold, italic, underline, strikethrough, foreground color, heading levels (H2/H3), bulleted list
- [ ] Option D: Full — all common formatting including link insertion, font size, background color
- [ ] Other: ___

**Q013**: Should the editor validate Google Drive preview URLs in the `preview` field?
> **Why this matters:** An invalid preview URL causes the item to be silently invisible in the gallery (`extractId()` returns null); validation would catch such errors before saving.
- [x] Option A: Regex pattern validation — check that `preview` value matches the expected Google Drive URL format; show inline warning if not *(recommended)*
- [ ] Option B: Live network validation — attempt to fetch the URL and report its HTTP status
- [ ] Option C: No validation — save as-is regardless of URL format
- [ ] Other: ___

**Q014**: How should item deletion work?
> **Why this matters:** Without confirmation, accidental deletion of items would require re-uploading the original file to recover, since there is no undo stack.
- [x] Option A: Delete button with a confirmation dialog before removal *(recommended)*
- [ ] Option B: Delete button without confirmation; relies on undo (if implemented via Q010) for recovery
- [ ] Option C: Soft delete — mark item as deleted visually; remove physically only on save
- [ ] Other: ___

**Q015**: How should adding new items to an event work?
> **Why this matters:** The placement and method for inserting new items determines how naturally the user can build the narrative flow of photos and text within an event.
- [ ] Option A: Global add buttons — "Add photo entry" and "Add text block" buttons at the top and/or bottom of the item list
- [ ] Option B: Between-item insert buttons — small insert buttons between each pair of existing items
- [x] Option C: Both — global add buttons for appending plus between-item insert buttons for specific positioning *(recommended)*
- [ ] Other: ___

**Q016**: Should the editor allow editing multiple events in the same browser session?
> **Why this matters:** Determines whether unsaved changes in one event are preserved or discarded when the user switches to edit a different event.
- [x] Option A: One event at a time — switching events prompts to save unsaved changes first *(recommended)*
- [ ] Option B: Tabbed multi-event — a tab bar within the editor allows multiple events open simultaneously
- [ ] Option C: One event at a time — switching discards unsaved changes without prompt
- [ ] Other: ___

**Q017**: Should the editor support creating new event JS files?
> **Why this matters:** Supporting new file creation adds scope: the editor would also need to register the new event in `events.js` or at least generate a correctly structured empty file.
- [ ] Option A: Create new event file from scratch — enter filename and slug; editor generates an empty `window.GALLERY_ITEMS = [];` file
- [ ] Option B: Clone an existing event file as a starting template under a new filename
- [x] Option C: No new event file creation — edit existing files only *(recommended)*
- [ ] Other: ___

**Q018**: How large should photo/video preview thumbnails be displayed in the editor?
> **Why this matters:** Larger previews make it easier to identify photos during editing but require more scrolling on events with 50+ items; smaller thumbnails improve scanning speed.
- [ ] Option A: Small thumbnails (~150px height) — more items visible per viewport
- [x] Option B: Medium previews (~300–400px height) — balance between visibility and density *(recommended)*
- [ ] Option C: Large previews (~600px+ height) — easy to inspect detail, more scrolling required
- [ ] Option D: User-configurable zoom level via a slider or setting
- [ ] Other: ___

**Q019**: Should the editor validate item fields before allowing the file to be saved?
> **Why this matters:** Empty or malformed required fields (e.g., blank `preview` URL) silently break gallery display; validation can catch these before the file is saved.
- [x] Option A: Warn — display a list of validation warnings before/after save but do not block the operation *(recommended)*
- [ ] Option B: Block — prevent save until all items pass validation; show inline errors per item
- [ ] Option C: No validation — save as-is regardless of field state
- [ ] Other: ___

**Q020**: How should the editor load the event list to populate the event navigation?
> **Why this matters:** The mechanism determines whether the user must manually provide `events.js` or whether the editor can discover events automatically from a folder selection.
- [x] Option A: User uploads `events.js` once at session start to populate the event navigation list *(recommended)*
- [ ] Option B: User selects the `data/` folder via File System Access API; `events.js` and all event files are read automatically
- [ ] Option C: Hardcode the event list inside the editor HTML file; no `events.js` loading needed
- [ ] Other: ___

**Q021**: Should the editor allow changing the type of an existing item (e.g., converting a photo entry into a text block)?
> **Why this matters:** Allowing type changes enables flexible restructuring of event content; disallowing it keeps the item schema simpler and avoids accidental type conversions.
- [ ] Option A: Allow type change — a type selector lets the user switch an item between image, video, and text block types
- [x] Option B: Type is immutable per item — to change type, delete the item and add a new one of the desired type *(recommended)*
- [ ] Other: ___

**Q022**: Should the editor show a live rendered preview of text block content while editing?
> **Why this matters:** A rendered preview helps the user see how formatted `body` text will appear in the gallery without having to open the gallery viewer separately.
- [ ] Option A: Live side-by-side preview panel — rendered preview updated in real time as the user types
- [ ] Option B: Toggle preview — a button to switch the item card between edit and preview modes *(recommended)*
- [x] Option C: No preview — edit raw text or markup only
- [ ] Other: ___

**Q023**: Should the editor work fully offline (no internet connection required)?
> **Why this matters:** Offline editing is a realistic scenario (e.g., editing travel photos while travelling); using CDN-loaded libraries would break the editor without internet access.
- [x] Option A: Fully offline — no CDN dependencies; all code embedded inline in the HTML file *(recommended)*
- [ ] Option B: CDN with graceful degradation — load optional libraries from CDN; fall back to reduced functionality when offline
- [ ] Option C: CDN required — load libraries from CDN; internet connection required
- [ ] Other: ___

**Q024**: Should the editor show a visible "unsaved changes" indicator in the UI?
> **Why this matters:** A visible indicator prevents the user from accidentally closing the editor or switching events without realising they have pending unsaved changes.
- [x] Option A: Visible badge/indicator — display "Unsaved changes" label in the editor toolbar whenever there are pending changes *(recommended)*
- [ ] Option B: `beforeunload` warning only — no in-editor indicator; browser warns only when the tab is closed with unsaved changes
- [ ] Option C: No unsaved-changes indicator
- [ ] Other: ___

**Q025**: How should photo and video entries be previewed in the editor while editing?
> **Why this matters:** Google Drive iframes load slowly and may time out for 50+ items; a lightweight thumbnail approach improves editor performance at the cost of lower preview fidelity.
- [ ] Option A: Google Drive iframe preview — embed the full Google Drive preview iframe for each media item (same as current `comments_app.js`)
- [x] Option B: Thumbnail image only — use the Google Drive thumbnail URL as a fast `<img>` tag instead of an iframe *(recommended)*
- [ ] Option C: No preview — show only filename and URL text fields; no visual media preview
- [ ] Other: ___
