# Analysis: R-20260719-1206

**Request ID:** R-20260719-1206

**Request title:** Create local editor for 202507_china/data event JS files

---

## Overview

### Background

The `202507_china/` gallery stores all event data as JavaScript files under `202507_china/data/`. Each file defines `window.GALLERY_ITEMS`, an array of photo entries (`name`, `preview`, `desc`) and text block entries (`type`, `title`, `body`, `desc`). An existing partial editor (`comments_index.html` + `comments_app.js`) supports upload/download of single event files and exposes limited field editing: only `desc` for media items, and `type`, `title`, `body`, `desc` for text blocks. The developer requires a fully-featured local editor with complete field coverage for all item attributes, rich text formatting in text fields, item management (add, delete, reorder), and multi-event navigation — all running purely in a local browser with no server.

### Scope

- New or enhanced HTML editor targeting all files in `202507_china/data/`
- Full field editing for photo/video entries: `name`, `preview`, `desc`, and any new fields (e.g., `size`)
- Full field editing for text block entries: `type`, `title`, `body`, `desc`
- Rich text formatting support in text fields (bold, colors, newlines)
- Item creation, deletion, and reordering within an event
- Event file navigation across all 47 events
- All functionality operating locally in a browser via `file://` protocol without server-side code

### Out of scope

- Publishing or deploying changes to GitHub Pages
- Server-side components or build tools
- Editing non-gallery files (root HTML pages, CSS, or JS files outside `202507_china/data/`)
- Uploading or managing media files on Google Drive
- Editing the gallery viewer (`app.js`, `style.css`) except where strictly required by the chosen rich text format approach

---

## Input Interpretation

### User Original Inputs

> Create a local editor of the files in `202507_china/data` where I can add and edit text comments to the photos. All aspects of the js files should be able to be edited - photos, size, text comments, text formatting (new lines, bold, coloring, etc.), should be able to edit all attributes of photos, text and other types of entries - full edit control. The editor should be able to be loaded in my local browser (no publishing - I will edit everything locally).

### AI interpretation

The developer requests a browser-based, fully local editor targeting the per-event JavaScript data files in `202507_china/data/`. The editor must provide complete CRUD control over `window.GALLERY_ITEMS` arrays in each event file: reading file content, rendering all items in an editable UI, allowing creation, editing, deletion, and reordering of items, and writing modified content back to disk.

The editor must expose all currently defined fields per item type:

- **Media entries** (image/video): `name` (filename reference), `preview` (Google Drive URL), `desc` (caption/description), and a new `size` field if the developer confirms its intended meaning
- **Text block entries** (`type: text`): `type`, `title`, `body`, `desc`

The phrase "text formatting (new lines, bold, coloring, etc.)" indicates that `desc` and `body` text fields must support inline formatting. The current gallery viewer (`app.js`) escapes all text content via `esc()` and only converts `\n` to `<br>`. Supporting bold and color would require either storing HTML markup in the string fields and updating the gallery viewer, or adopting a Markdown or custom syntax. The preferred approach is unresolved and must be clarified.

"Loaded in my local browser" means the editor must function as a pure static file via the `file://` protocol with no server. File I/O must use browser-native APIs (File API or File System Access API). The project convention (from `context.md`) mandates pure static HTML, CSS, and vanilla JavaScript with no build toolchain.

The project has an existing partial editor (`comments_index.html` / `comments_app.js`). Whether the new editor replaces, extends, or coexists with it is unresolved and must be clarified.

**Resolved decisions (from answered Q-blocks Q001–Q025):** The developer confirmed the following implementation choices: File System Access API for in-place read/write (Q001-B); HTML markup stored in `body`/`desc` fields (Q002-A); `app.js` updated to render HTML via `innerHTML` (Q003-A); no `size` field added (Q004-D); `events.js` registry editing included (Q005-A); new standalone `data_editor.html` file (Q006-A); dropdown event navigation (Q007-A); move-up/down reorder buttons (Q008-B); manual-only save (Q009-A); native browser undo only (Q010-A); `contentEditable` + `execCommand` rich text toolbar (Q011-A); extended formatting scope — bold, italic, underline, strikethrough, foreground color, H2/H3, bulleted list (Q012-C); regex URL validation with warnings (Q013-A); delete with confirmation dialog (Q014-A); both global and between-item insert controls (Q015-C); one event at a time with save prompt on switch (Q016-A); no new event creation (Q017-C); medium (~300–400px) thumbnail previews (Q018-B); warn-only save validation (Q019-A); `events.js` loaded from directory handle at session start (Q020-A); type is immutable per item (Q021-B); no live content preview in editor (Q022-C); fully offline, no CDN (Q023-A); visible unsaved-changes badge (Q024-A); Google Drive thumbnail `<img>` tags instead of iframes (Q025-B).

---

## Research Results

### Workspace pattern-scan

**Impacted components:**

- `202507_china/comments_index.html` — existing partial editor entry point; limited field editing
- `202507_china/comments_app.js` — existing editor logic; upload/download, parse, render, regenerate
- `202507_china/app.js` — gallery viewer; renders gallery items; currently escapes all text (prevents HTML rendering in desc/body)
- `202507_china/data/events.js` — event registry; defines `window.GALLERY_EVENT_INDEX` (48 entries)
- `202507_china/data/event-*.js` — 45 per-event data files, each defining `window.GALLERY_ITEMS`
- `202507_china/data/event-20250724-template.js` — empty template file with placeholder items
- `.aib_memory/context.md` — must be updated to document the new editor component

**Existing editor capabilities (`comments_app.js`):**

- Parses `window.GALLERY_ITEMS` from pasted or uploaded JS text using a custom regex-based parser
- Renders Google Drive iframes inline for media entries (image and video)
- Editable `desc` field for media entries
- Editable `type`, `title`, `body`, `desc` for text block entries
- Per-item include/exclude toggle (commented-out items use `//` line prefix)
- Insert text block above a media item
- File upload from disk (FileReader API), download to disk (Blob + anchor)
- Live regeneration of output JS in the source textarea on every change

**Gaps versus requested scope:**

- `name` and `preview` fields are not editable for media entries
- No rich text or formatting support in any field
- No item reordering
- No event navigation; one file at a time, manual upload/download per event
- No `size` or extended attribute support
- No way to add a new media item (only text blocks can be inserted above an existing media item)

**Cross-file invariants:**

- `window.GALLERY_ITEMS` array format must be maintained; any changes must preserve property ordering and quoting style used in existing files
- Items commented out with `//` prefix are still part of the file; the parser must detect and preserve them
- `events.js` defines `window.GALLERY_EVENT_INDEX`; any navigation feature that loads events dynamically depends on this file

### Industry knowledge

**1. File System Access API (WICG/W3C, Chrome 86+):**
The File System Access API (`showOpenFilePicker`, `showSaveFilePicker`, `showDirectoryPicker`) allows web pages opened via `file://` or `https://` to request persistent read/write access to files on disk. Using `showDirectoryPicker`, the editor can read all `.js` files in the `data/` folder and write back changes in place without any upload/download cycle. Key limitation: not supported in Firefox (status: `under consideration` in Mozilla's web-compat tracker, not shipped as of 2025). Applicability: highly relevant when the developer uses Chrome or Edge; requires graceful fallback for Firefox users.

**2. Rich text editing in vanilla JavaScript (WHATWG / MDN standards):**
The `contentEditable` attribute combined with custom keyboard event handlers and a formatting toolbar allows WYSIWYG editing without external libraries. `document.execCommand()` (bold, foreColor, etc.) is deprecated in the WHATWG living standard but continues to work in all major browsers. The preferred modern approach is a `contentEditable` element with `document.execCommand()` for simple formatting needs, accepting the deprecation notice for a local-only tool. For Markdown-based editing, a plain `<textarea>` with a live preview div (rendered via a small Markdown parser) provides a no-library alternative. Applicability: both approaches are viable for a local-only tool with no CDN dependency requirement.

**3. Local-first editing patterns (Decap CMS, Publii, static-site tooling):**
Projects like Decap CMS (formerly Netlify CMS) and Publii follow a data-model-first editing pattern: parse file → build internal model → render editable widgets → serialize model back on save. The consistent recommendation is strict separation of the data model (JS objects) from the UI rendering and from the JS serialization layer. The current `comments_app.js` partially implements this pattern but uses the source textarea as both model and output, which creates tight coupling. Applicability: a clean data model layer would improve reliability of the new editor.

**4. JavaScript object serialization for static-site data files:**
Reconstructing `window.X = [...]` output from an edited model requires a dedicated serializer that preserves property order, quoting style, and indentation. The existing `buildOutput()` function in `comments_app.js` handles the current limited schema correctly using template strings and a `jsStringEscape()` helper. Extending this to new fields requires updating `buildOutput()` and ensuring the `jsStringEscape()` function handles all characters that may appear in user-entered text (including Unicode, HTML entities if HTML markup is used, and control characters).

**5. XSS considerations for stored HTML in data files (OWASP Top 10 — A03: Injection):**
If formatted HTML is stored in `desc` or `body` fields, the gallery viewer rendering those fields with `innerHTML` introduces a stored XSS risk within the context of the gallery page. For a local-only, single-user tool with no public-facing deployment, the practical risk is low. However, the current `esc()` function in `app.js` would need to be replaced with a safe HTML rendering approach (either trusted-content bypass with no sanitizer, or a DOMPurify-style sanitizer). Since `comments_app.js` already uses `escapeHtml()` for display and `jsStringEscape()` for output, the serialization layer is safe; the risk is in the gallery viewer consumption.

### AI Agent critique

- `202507_china/data/events.js`: Contains a duplicate entry for `event-20250724-tudja`; two identical `{ slug: "event-20250724-tudja", title: "20250724_08 Племето Туджа от народа Ба", data: "..." }` entries. The gallery dropdown will show this event twice. Likely a data entry error.

- `202507_china/data/events.js`: Event `event-20250726_08-3gorges` has an empty `title: ""`. The gallery dropdown will show a blank label for this event.

- `202507_china/comments_app.js` — `tryParseItems()`: Uses a broad regex `([{\,]\s*)([a-zA-Z_$][\w$]*)\s*:` to add quotes to unquoted property names. This will fail for any property name that starts mid-line without a preceding `{` or `,`, e.g., on indented continuation lines. Also strips trailing commas in `arrStrClean` generation but `arrStrQuoted` trailing-comma stripping via `/([\s]*[}\]])` is overly permissive.

- `202507_china/comments_app.js` — `tryParseItems()`: The `_checked` flag mapping uses `jsonLines[idx]?.checked`, but `idx` is the loop index in `raw.map((it, idx) => ...)` from the parsed JSON array. If any `jsonLines` entries were filtered out (e.g., due to empty lines being skipped), the indices will be misaligned, resulting in incorrectly marked checked/unchecked states.

- `202507_china/comments_app.js` — `jsStringEscape()`: Does not handle Unicode surrogate pairs, null bytes (`\0`), or non-breaking spaces that may appear in imported text from external sources (e.g., copied from a web page). These could produce malformed output JS.

- `202507_china/app.js` — `makeTextCard()`: Uses `esc(item.body).replace(/\n/g,'<br>')`. This approach is incompatible with HTML-formatted content: if `body` stores `<b>bold</b>`, `esc()` will output `&lt;b&gt;bold&lt;/b&gt;`, rendering literally rather than as bold text.

- `202507_china/app.js` — `makeImageCard()`: The `img.onerror` fallback retries with `thumb(item.id, 1600)`, but if `item.id` was already derived from the same URL and is valid, the retry is likely to succeed; however if `extractId()` failed and returned `null`, neither URL is valid and the error is silently swallowed.

- `202507_china/comments_app.js`: The `render()` function calls `regenerate(items)` at the end, which overwrites the source textarea with regenerated JS. If the user has partially typed unsaved content in the textarea before clicking "Upload", the upload handler calls `src.value = content` and then `autoLoadItems()` which calls `render()` → `regenerate()`, which is correct. However, if the user edits the textarea directly (typing) and those edits trigger `autoLoadItems()`, the parse may fail on invalid intermediate states, clearing the rendered grid.

### Edge Cases

- **Empty event files**: `window.GALLERY_ITEMS = [];` — editor must handle gracefully without crashing.
- **Commented-out items at parse time**: Items with `//` prefix must be parsed, displayed with a "disabled" visual indicator, and preserved in output.
- **Large events**: Some events have 50+ items (e.g., `event-20250721_05-lui-vuiton.js` has ~60 items); the editor's rendering must remain performant.
- **Unsaved changes on event switch**: If the user switches to a different event without saving, changes could be lost; a confirmation prompt or auto-save is required.
- **HTML injection risk**: If HTML markup is stored in `body`/`desc` and rendered via `innerHTML` in the gallery viewer without sanitization, stored XSS is possible in the gallery page context.
- **Invalid Google Drive URLs**: A `preview` value not matching `https://drive.google.com/file/d/<id>/preview` causes `extractId()` in `app.js` to return `null`, making the item invisible in the gallery.
- **File System API browser support**: The File System Access API is unavailable in Firefox; the editor must either detect this and fall back to File API, or document the requirement.
- **Encoding edge cases**: Cyrillic, emoji, and other non-ASCII characters are already present in data files (confirmed in event titles and `desc` fields); the serializer must output these without escaping them to `\uXXXX`.
- **Missing optional fields in existing files**: If a new `size` field is introduced, all existing event files lack it; the editor must gracefully default missing optional fields.
- **Concurrent external edits**: If a file is edited outside the editor while it is open, the editor has no mechanism to detect the external change.
- **Template file**: `event-20250724-template.js` contains placeholder items; the editor should load it without special-casing but the user should be aware it is a template.

### Requirements Gate Evaluation

Requirements Gate: 8/8 PASS — all categories satisfied.

---

## Proposed Solution

### High-Level Concept

Build a new standalone single-file HTML editor (`202507_china/data_editor.html`) using pure vanilla JavaScript and CSS that allows the developer to load, edit, and save all per-event gallery data files and `events.js` in `202507_china/data/` via the browser File System Access API (`showDirectoryPicker()`). The editor exposes all item fields with a rich text toolbar (`contentEditable` + `document.execCommand()`) storing HTML markup directly in `body`/`desc` fields, provides full item management (add, delete with confirmation, move-up/down reorder), event navigation via dropdown, and `events.js` registry editing, all running offline in Chromium-based browsers with no CDN dependencies.

### Execution Steps

#### Task 1: Create the editor entry point

- `202507_china/data_editor.html`: Create new standalone HTML file containing the complete editor (inline CSS and inline JavaScript); include semantic layout: header with folder-open button, event selector dropdown, unsaved-changes badge, formatting toolbar, and Save button; main content area (scrollable item list); delete-confirmation dialog; events.js registry editor panel.

#### Task 2: Implement folder access and event registry loading

- `202507_china/data_editor.html` (inline script): Implement `showDirectoryPicker()` invocation on folder-open button click; store the resulting directory handle; request read/write permission on the handle.
- `202507_china/data_editor.html` (inline script): Read `events.js` from the directory handle; parse `window.GALLERY_EVENT_INDEX` from the file text; populate the event navigation `<select>` dropdown with event titles.
- `202507_china/data_editor.html` (inline script): Implement events.js editor panel — list event entries (slug, title, data path) in an editable table; support add entry (append row), remove entry (with `window.confirm()`), reorder entries (up/down buttons), and inline editing of slug/title/data-path fields; Save button writes regenerated `window.GALLERY_EVENT_INDEX = [...];` back to `events.js` via File System Access API write handle.
- `202507_china/data_editor.html` (inline script): On event dropdown change, if dirty state is false load the selected event file from the directory handle and render its items; if dirty state is true show a `window.confirm()` prompt before switching.

#### Task 3: Implement item editor widgets

- `202507_china/data_editor.html` (inline script): Implement editable card for media entries (image/video) exposing `name` (text input), `preview` (text input with inline Google Drive URL regex validation warning on blur), and `desc` (contentEditable rich text div) fields; display a Google Drive thumbnail `<img>` derived from the `preview` URL at ~300–400px height.
- `202507_china/data_editor.html` (inline script): Implement editable card for text block entries exposing `type` (read-only label — type is immutable), `title` (plain text input), `body` (contentEditable rich text div), and `desc` (contentEditable rich text div) fields; no live rendered preview.
- `202507_china/data_editor.html` (inline script): Implement shared rich text formatting toolbar with buttons for: Bold, Italic, Underline, Strikethrough, Foreground Color (color picker), H2 heading, H3 heading, and Bulleted List; each button invokes `document.execCommand()` against the currently focused `contentEditable` element; toolbar is shared across all rich text fields in all visible item cards.

#### Task 4: Implement item management controls

- `202507_china/data_editor.html` (inline script): Render global "Add photo entry" and "Add text block" buttons both at the top and at the bottom of the item list for appending new items.
- `202507_china/data_editor.html` (inline script): Render small between-item insert controls between each adjacent pair of items; each control offers insert-photo and insert-text-block actions for position-specific insertion.
- `202507_china/data_editor.html` (inline script): Render a Delete button on each item card; clicking invokes `window.confirm()` identifying the item index and type; remove item from model array only on confirmation.
- `202507_china/data_editor.html` (inline script): Render Move-up and Move-down arrow buttons on each item card; clicking swaps the item with its adjacent neighbour in the model array and re-renders the affected cards.
- `202507_china/data_editor.html` (inline script): Track a `dirty` boolean in module scope; set true on any field edit or structural change (add, delete, reorder); set false after successful save; display an "Unsaved changes" badge in the editor header whenever `dirty` is true.

#### Task 5: Implement serialization and save

- `202507_china/data_editor.html` (inline script): Implement `buildOutput()` serializer generating `window.GALLERY_ITEMS = [\n  {...},\n]\n`; preserve property key ordering and double-quote strings; implement a `jsStringEscape()` helper that escapes backslashes, double quotes, and control characters but preserves Unicode (Cyrillic, emoji) as literal characters without `\uXXXX` encoding; preserve HTML markup characters (`<`, `>`, `&`) as-is within string values so that stored HTML renders via `innerHTML` in the gallery.
  - Sub-note: HTML in `body`/`desc` must survive serialization intact; the serializer must not double-escape angle brackets or ampersands in string field values.
- `202507_china/data_editor.html` (inline script): Implement Save button handler that calls `buildOutput()` then writes via `FileSystemFileHandle.createWritable()` → `write()` → `close()` to the currently open event file; clear `dirty` state on success; display a brief save-success notification.
- `202507_china/data_editor.html` (inline script): Implement pre-save validation: iterate all items and collect media entries with missing `preview` and entries whose `preview` does not match `^https://drive\.google\.com/file/d/[a-zA-Z0-9_-]+/preview$`; if violations exist display a dismissible warning list before saving; do not block save.

#### Task 6: Update gallery viewer for rich text rendering

- `202507_china/app.js`: In `makeTextCard()`, replace `esc(item.body).replace(/\n/g,'<br>')` with direct `innerHTML` assignment so that HTML stored in `body` renders as formatted text.
  - Sub-note: HTML in `body`/`desc` is authored only through the trusted local editor; no public write path exists for this single-author personal site; no sanitizer is required.
- `202507_china/app.js`: In `makeImageCard()`, replace the `esc(item.desc)` assignment with `innerHTML` assignment so formatted captions render correctly in the gallery.

#### Task 7: Update context.md

- `.aib_memory/context.md`: Add `data_editor.html` to `## Solution`; add `data_editor.html` to `## File Structure` under `202507_china/`; add HTML-markup-in-body/desc concept to `## Concepts`; add MUST requirement for `app.js` `innerHTML` rendering to `## Requirements`.

---

## Context Update Analysis

### Context Elements to Add

- `## Solution`: Add bullet — `data_editor.html is a standalone full-featured local editor for 202507_china/data/ event JS files; uses File System Access API for in-place read/write of event JS files and events.js; supports HTML rich text in desc/body fields via contentEditable toolbar, full item CRUD, move-up/down reorder, and events.js registry editing; runs fully offline in Chromium-based browsers.`
- `## File Structure` under `202507_china/`: Add entry — `data_editor.html — standalone full-featured local editor for data/*.js event JS files and events.js; requires Chromium-based browser for File System Access API`
- `## Concepts`: Add bullet — `HTML markup stored directly in body and desc string fields of gallery event data files; rendered via innerHTML in app.js gallery viewer; authored only through the trusted local data_editor.html editor.`
- `## Requirements`: Add MUST rule — `MUST: app.js MUST render body and desc fields of gallery items via innerHTML to support stored HTML rich text formatting.`

### Context Elements to Modify/Remove

- No existing context entries require modification or removal. The `comments_app.js` utility entry in `## Solution` is retained unchanged; the new editor is an additive component, not a replacement. The existing `comments_app.js` and `comments_index.html` entries in `## File Structure` are retained unchanged.
- The existing `## Concepts` entry describing `window.GALLERY_ITEMS` with `name`, `preview`, and `desc` fields requires no change (no new schema fields introduced; Q004 confirmed no `size` field).

### Conflict Resolution & Intent Preservation

- The existing MUST requirement `MUST: All pages and features must operate as static files with no server-side code` is fully preserved; `data_editor.html` is a static HTML file with no server dependency.
- The existing MUST requirement `MUST: Each gallery event data file must define window.GALLERY_ITEMS array with items containing name, preview, and desc fields` is preserved and unaffected; the editor reads and writes exactly this schema.
- The new MUST requirement for `app.js` to use `innerHTML` for rendering is an additive rule; it does not conflict with any existing requirement. The XSS risk is bounded to a single-author personal site with no public write path.
- The existing `comments_app.js` solution entry is not overwritten; both tools coexist serving different scopes (quick upload/preview utility vs. full editor).

---

## Decision Register

### Decision Points

#### Decision Point: file-access-mechanism

- Tag: resolved-by-user
- Resolution: Option B selected (Q001). The editor uses the File System Access API (`showDirectoryPicker()`) for direct folder access with in-place file read/write. Requires a Chromium-based browser (Chrome 86+, Edge 86+); Firefox is not supported.

Chosen alternative:

- Option B: File System Access API — `showDirectoryPicker()` for direct folder access; read/write in place; Chromium-based browsers only (Chrome 86+, Edge 86+; not Firefox)

---

#### Decision Point: rich-text-storage-format

- Tag: resolved-by-user
- Resolution: Option A selected (Q002). HTML markup is stored directly in `body` and `desc` string fields. The gallery viewer (`app.js`) must render these fields via `innerHTML`.

Chosen alternative:

- Option A: Store HTML markup directly in string fields (e.g., `<b>bold</b>`, `<span style="color:red">text</span>`)

---

#### Decision Point: gallery-viewer-update-for-richtext

- Tag: resolved-by-user
- Resolution: Option A selected (Q003). `app.js` `makeTextCard()` (and `makeImageCard()` desc rendering) will be updated to render `body` and `desc` as HTML via `innerHTML`.

Chosen alternative:

- Option A: Yes — update `app.js` `makeTextCard()` (and `makeImageCard()` desc) to render formatted content from `body`/`desc` fields via `innerHTML`

---

#### Decision Point: size-field-meaning

- Tag: resolved-by-user
- Resolution: Option D selected (Q004). No `size` field is added to the schema. The editor does not expose a size attribute for media entries.

Chosen alternative:

- Option D: No size field — user does not want to add one; the mention was informal

---

#### Decision Point: events-js-editing

- Tag: resolved-by-user
- Resolution: Option A selected (Q005). The editor includes a `events.js` registry editor panel allowing the user to add, remove, reorder, and edit event entries (slug, title, data path).

Chosen alternative:

- Option A: Include `events.js` editing — allow adding, removing, and reordering event entries; edit slug, title, and data path per entry

---

#### Decision Point: editor-architecture

- Tag: resolved-by-user
- Resolution: Option A selected (Q006). A new standalone file `202507_china/data_editor.html` is created. The existing `comments_index.html` and `comments_app.js` are retained unchanged; both files coexist.

Chosen alternative:

- Option A: New standalone file — create `202507_china/data_editor.html` alongside the existing `comments_index.html`; both files coexist

---

#### Decision Point: event-navigation

- Tag: resolved-by-user
- Resolution: Option A selected (Q007). A `<select>` dropdown populated from `events.js` is used for event navigation.

Chosen alternative:

- Option A: Dropdown — a `<select>` dropdown populated from the loaded event registry listing all events by title; user selects to load

---

#### Decision Point: item-reordering

- Tag: resolved-by-user
- Resolution: Option B selected (Q008). Move-up and move-down arrow buttons are rendered per item for reordering within an event.

Chosen alternative:

- Option B: Move-up / move-down arrow buttons per item

---

#### Decision Point: auto-save-vs-manual-save

- Tag: resolved-by-user
- Resolution: Option A selected (Q009). An explicit Save button writes the file in place via File System Access API. No automatic writes occur.

Chosen alternative:

- Option A: Manual save only — explicit Save button; no automatic file writes

---

#### Decision Point: undo-redo

- Tag: resolved-by-user
- Resolution: Option A selected (Q010). Native browser undo (`Ctrl+Z`) within text input fields only; no custom undo stack for structural changes (add, delete, reorder).

Chosen alternative:

- Option A: Native browser undo (`Ctrl+Z`) within text input fields only — no custom implementation

---

#### Decision Point: rich-text-editor-implementation

- Tag: resolved-by-user
- Resolution: Option A selected (Q011). `contentEditable` divs with `document.execCommand()` for bold, italic, foreground color, etc.; no external library; works fully offline.

Chosen alternative:

- Option A: `contentEditable` div with a custom formatting toolbar using `document.execCommand()` — no external library, fully inline

---

#### Decision Point: rich-text-options-scope

- Tag: resolved-by-user
- Resolution: Option C selected (Q012). Extended formatting set: bold, italic, underline, strikethrough, foreground color, H2/H3 heading levels, and bulleted list.

Chosen alternative:

- Option C: Extended — bold, italic, underline, strikethrough, foreground color, heading levels (H2/H3), bulleted list

---

#### Decision Point: url-validation

- Tag: resolved-by-user
- Resolution: Option A selected (Q013). Regex pattern validation checks that `preview` values match the expected Google Drive URL format; an inline warning is shown for non-matching values; save is not blocked.

Chosen alternative:

- Option A: Regex pattern validation — check that `preview` value matches `https://drive.google.com/file/d/<id>/preview` format; show inline warning if not

---

#### Decision Point: item-deletion

- Tag: resolved-by-user
- Resolution: Option A selected (Q014). Each item card has a Delete button that triggers a `window.confirm()` dialog before removing the item from the model.

Chosen alternative:

- Option A: Delete button with a confirmation dialog before removal

---

#### Decision Point: new-item-creation

- Tag: resolved-by-user
- Resolution: Option C selected (Q015). Both global add buttons (at top and bottom of list) and between-item insert buttons are implemented for maximum placement flexibility.

Chosen alternative:

- Option C: Both — global add buttons for appending plus between-item insert buttons for specific positioning

---

#### Decision Point: multi-event-session

- Tag: resolved-by-user
- Resolution: Option A selected (Q016). One event is active at a time. Switching events while unsaved changes exist prompts the user via `window.confirm()` before discarding.

Chosen alternative:

- Option A: One event at a time — switching events prompts to save unsaved changes first

---

#### Decision Point: new-event-creation

- Tag: resolved-by-user
- Resolution: Option C selected (Q017). The editor supports editing existing event files only; creating new event JS files from scratch is out of scope.

Chosen alternative:

- Option C: No new event creation — edit existing files only

---

#### Decision Point: grid-size-in-editor

- Tag: resolved-by-user
- Resolution: Option B selected (Q018). Google Drive thumbnails displayed at ~300–400px height, balancing visibility with scrolling density for events with many items.

Chosen alternative:

- Option B: Medium previews (~300–400px height) — balance between visibility and density

---

#### Decision Point: validation-on-save

- Tag: resolved-by-user
- Resolution: Option A selected (Q019). Pre-save validation collects invalid or missing fields and shows a warning list; save is not blocked.

Chosen alternative:

- Option A: Warn — display a list of validation warnings before/after save but do not block the save operation

---

#### Decision Point: events-loading-for-navigation

- Tag: resolved-by-user
- Resolution: Option A selected (Q020). With file-access-mechanism resolved to File System Access API (Q001-B), `events.js` is read directly from the directory handle obtained via `showDirectoryPicker()` at session start, which satisfies the intent of Option A (load once per session). Per-event files are also read from the same directory handle.

Chosen alternative:

- Option A: `events.js` loaded once at session start from the directory handle; per-event files accessed from the same handle

---

#### Decision Point: type-field-editing-for-media

- Tag: resolved-by-user
- Resolution: Option B selected (Q021). Item type is immutable. To change an item's type, the user deletes the item and creates a new one of the desired type.

Chosen alternative:

- Option B: Type is immutable per item — to change type, delete the item and add a new one of the desired type

---

#### Decision Point: text-block-content-preview

- Tag: resolved-by-user
- Resolution: Option C selected (Q022). No rendered preview in the editor; users edit HTML markup directly in the `contentEditable` fields; preview is available in the gallery viewer.

Chosen alternative:

- Option C: No preview — edit raw text or markup only; no rendered preview in the editor

---

#### Decision Point: CDN-vs-offline

- Tag: resolved-by-user
- Resolution: Option A selected (Q023). The editor is fully offline-capable; all code is embedded inline in the HTML file; no CDN dependencies; works on `file://` with no internet connection.

Chosen alternative:

- Option A: Fully offline — no CDN dependencies; all code embedded inline in the HTML file; works on `file://` with no internet connection

---

#### Decision Point: unsaved-changes-indicator

- Tag: resolved-by-user
- Resolution: Option A selected (Q024). A visible "Unsaved changes" badge is displayed in the editor header whenever dirty state is active.

Chosen alternative:

- Option A: Visible badge/indicator — display "Unsaved changes" label in the editor toolbar that appears when any change is made

---

#### Decision Point: photo-preview-in-editor

- Tag: resolved-by-user
- Resolution: Option B selected (Q025). Google Drive thumbnail images shown using `<img>` tags with the derived thumbnail URL at ~300–400px height; faster loading than iframes especially for events with 50+ items.

Chosen alternative:

- Option B: Thumbnail only — use the Google Drive thumbnail URL (`https://drive.google.com/thumbnail?id=<id>&sz=w400`) as an `<img>` tag; faster loading than iframes
