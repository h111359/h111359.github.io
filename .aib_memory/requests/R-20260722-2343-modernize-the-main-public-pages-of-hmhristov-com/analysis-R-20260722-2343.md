## Overview

- Request ID: R-20260722-2343
- Request title: Modernize the main public pages of hmhristov.com

### Background

The developer wants the main public presentation of hmhristov.com to communicate Hristo M. Hristov's senior data and analytics leadership clearly while also presenting his two vocabulary tools and complete artwork collection with greater credibility, accessibility, consistency, responsiveness, security, and search-and-sharing quality.

### Scope

The request covers the redesign of `index.html`, `cv.html`, `apps.html`, `art_drawing.html`, and `css/main.css`; it may add a small shared script such as `js/site.js` and only those static image derivatives that produce a material benefit. The work includes content editing, resilient shared navigation, page-specific metadata, a concise printable professional profile, two supported project cards, a complete medium-grouped art gallery, responsive styling, progressive motion, accessibility review, static-site security review, and verification on desktop, mobile, print, reduced-motion, and no-JavaScript paths.

### Out of scope

The vocabulary applications, China gallery, Markdown reader, checklist, links utility, and other tools are not to be redesigned or promoted from the four pages. No file below `202507_china/`, existing artwork original, or existing portrait original may be modified; no framework, build system, backend, analytics, form service, external font, or new third-party JavaScript dependency may be introduced.

## Input Interpretation

### User Original Inputs

The developer supplied the following complete requirement set:

- Modernize the four main pages into a coherent static GitHub Pages career site built with plain HTML, CSS, and vanilla JavaScript.
- Lead with Hristo as a senior data and analytics leader while keeping personal tools and visual artwork as equal-quality secondary pillars.
- Help professional visitors rapidly understand seniority, focus, capability areas, the two vocabulary applications, the artwork, and professional contact paths.
- Change `index.html`, `cv.html`, `apps.html`, `art_drawing.html`, and `css/main.css`; allow only small necessary shared static support files, a shared script such as `js/site.js`, and useful optimized image derivatives.
- Preserve the public filenames and URLs, keep the site build-free and static, and exclude all changes below `202507_china/` and all redesign work for the vocabulary apps and other utilities.
- Use visible navigation labels Home, Professional, Projects, and Creative mapped respectively to `index.html`, `cv.html`, `apps.html`, and `art_drawing.html`; provide an active state, shared JavaScript enhancement, ordinary links, and functional semantic HTML fallback navigation.
- Make the homepage a first-person editorial gateway whose first screen contains Hristo's name, concise professional proposition, current portrait, primary LinkedIn action, secondary email action using the existing address, and routes to the other three pillars; follow it with equally intentional Professional, Projects, and Creative previews.
- Make the Professional page a concise third-person résumé rather than a complete chronology; prioritize leadership, data and analytics, architecture, organizational development, delivery, and hands-on depth; retain only supported facts; include education, skills, languages, certifications, and relevant evidence; and provide a clean print layout.
- Make the Projects page present exactly the two existing vocabulary applications with supported, consistent project summaries and direct launch links; do not change either application.
- Make the Creative page a first-person, complete presentation of all currently shown artwork, grouped by medium, with corrected English, natural proportions, responsive layout, meaningful alternative text, and only evidence-supported captions.
- Use a restrained warm editorial design with paper-like surfaces, dark legible text, an earthy accent, a quiet secondary surface, whitespace, hierarchy, consistent widths and components, system fonts, and related but pillar-specific layouts; avoid generic corporate styling, meters, dashboards, stock art, excessive gradients, clutter, and external fonts.
- Reflow at about 320 CSS pixels, preserve readable line lengths and image ratios, reserve image space, provide practical touch targets, and support current Chrome, Edge, Firefox, Safari, and corresponding mobile browsers.
- Add controlled entrance and scroll-based motion that progressively enhances visible content, never blocks access, respects reduced motion, and uses no animation dependency; do not rely on hover alone and provide visible keyboard focus.
- Apply a WCAG 2.2 AA-informed baseline without claiming formal conformance, covering semantics, language, headings, landmarks, skip navigation, native controls, keyboard behavior, focus, link purpose, image text alternatives, contrast, reflow, text spacing, targets, reduced motion, titles, and any retained data-table semantics.
- Keep primary navigation, content, and contact paths available when JavaScript fails.
- Give every page a unique accurate title, description, canonical URL, Open Graph title, description, URL, type, and appropriate social-preview metadata without keyword stuffing or unsupported claims.
- Keep LinkedIn as the primary contact path, the existing email as a secondary `mailto:` path, no contact form, proportionate privacy review, and safe relationship attributes on external links that open a new context.
- Add no secrets or third-party scripts, inline handlers, new injection points, or untrusted `innerHTML`; preserve safe protocols and avoid interfaces that imply nonexistent server processing.
- Preserve original images, create derivatives only when materially helpful, use stable predictable locations, provide intrinsic dimensions or aspect ratios, lazy-load below-fold images, and do not delay the principal portrait.
- Improve English and information hierarchy without inventing or materially changing facts, dates, qualifications, technologies, achievements, metrics, employers, roles, projects, awards, education, or current certification status.
- Use reusable header, navigation, footer, link/button, section, card, contact, gallery, active-state, reveal, and reduced-motion patterns while keeping shared JavaScript small and non-essential.
- Treat the 35 supplied acceptance criteria as binding, including coherent styling across all four pages; exact navigation labels and URLs; resilient navigation; professional homepage priority and three equal previews; portrait, LinkedIn, and email retention; concise factual résumé and print output; exactly two unchanged vocabulary apps; all 13 current artworks grouped by medium without cropping; no China-gallery link or file changes; unchanged originals; useful derivative use only where beneficial; complete page-specific metadata; no external font or third-party script; evergreen-browser and 320-pixel operation; keyboard focus and reduced motion; content availability on animation failure; semantic HTML; safe external links; no secrets; and build-free GitHub Pages deployment.
- In follow-up answers, the developer chose to preserve the legacy pages' published professional status; use a one-page executive profile; let the agent choose the most appropriate navigation enhancement; keep GitHub as a tertiary footer/profile link; use a rust-and-muted-sage palette; use subtle opacity-and-short-rise motion; use a natural-ratio artwork card grid with concise captions; and create optimized page-specific social-preview derivatives.

### AI interpretation

The implementation should replace each of the four pages with valid landmark-based HTML that contains its own usable header navigation and page content before scripts run. A single small `js/site.js` should enhance that authoritative markup in place with consistent active-location behavior and reveal classes, without replacing or reconciling navigation nodes, while leaving all essential links and content intact if the script or Intersection Observer is unavailable. New rules in `css/main.css` should be scoped to an explicit body class used only by the redesigned pages so that `cl.html`, `links_backup.html`, and `html_template.html`, which also import this stylesheet but are outside the request, retain their legacy presentation.

The content should be edited only from facts already present in the four pages and product context. The homepage should convert the existing third-person summary into a concise first-person leadership proposition while preserving the legacy page's published current-role and status wording without claiming independent verification. The Professional page should replace the detailed chronology with a one-page executive profile containing a summary, capability evidence, education, skills, languages, and credentials. The Projects page should describe the inline-data bidirectional PUK quiz and the dataset-backed HH Words quiz using only workspace-supported behavior. The Creative page should retain all 13 image references in a natural-ratio card grid with concise evidence-supported captions, separate ten oil works from three watercolors, correct the medium spelling, replace inaccurate alternatives such as the existing “Bezbog” label on a portrait and “Boat” label on two horses, and never force a common image height. GitHub should remain only as a tertiary footer or profile link so LinkedIn and email retain their primary and secondary hierarchy.

The visual layer should use cream paper surfaces, charcoal text, deep-rust primary accents, muted-sage secondary surfaces, constrained reading widths, clear responsive grids, intrinsic image dimensions, strong focus styles, one-time subtle opacity-and-short-rise reveals, print rules, and reduced-motion overrides. Existing assets are already practical for rendered page content: the referenced portrait is 600 × 800 and about 79 KB, and individual artwork files are 49–129 KB at 250–545 pixels wide. No display derivative is justified solely for download size, but four optimized page-specific social-preview derivatives are required for predictable sharing composition. The unused 3392 × 2544 portrait is about 3.16 MB and must not be substituted as a rendered-page image without optimization.

## Research Results

### Workspace Pattern Scan

- `index.html` currently supplies the live portrait reference, public LinkedIn and GitHub profile links, email address, professional summary, and interests. It has no declared page language, skip link, semantic header/navigation/main/footer, active location, canonical or social metadata, image alternative or dimensions, and contains invalid paragraph/div nesting plus a second opening `<body>` tag instead of a closing tag.
- `cv.html` contains the available employment, education, language, skill, and certification facts. It uses obsolete conditional markup, nested layout tables, an unclosed list item, repeated generic metadata, no semantic page landmarks or page language, and an empty-script request. Its complete chronology conflicts with the requested concise résumé.
- `apps.html` contains exactly the two approved application destinations. It starts content at an `h3`, provides names but no purpose or distinguishing information, duplicates the legacy menu, uses absolute self-links unnecessarily, and lacks unique metadata and semantic structure.
- `art_drawing.html` contains exactly 13 works: ten under Oil and three under the misspelled Watercolor heading. Visual inspection shows the existing alternative text is not consistently accurate: one “Bezbog” image is a portrait, one “Boat” image shows two running horses, “Makove” is untranslated, and duplicate generic labels do not distinguish works. The document also has invalid closing markup and no `h1`.
- `css/main.css` establishes an early warm palette but uses global element selectors and is also imported by `cl.html`, `links_backup.html`, and `html_template.html`. Fixed 400-pixel art height distorts aspect ratios; menu widths plus padding can overflow; fixed 14-point text, indentation margins, sticky navigation, nowrap links, hover-only movement, and absence of focus, reduced-motion, print, and narrow-screen rules do not meet the new baseline.
- `apps/PUK_English_Words.html` and `apps/hhwords/index.html` confirm that both projects are randomized English/Bulgarian multiple-choice learning tools. The former embeds its list and randomly switches translation direction; the latter loads `words.json`, includes transcription and example sentences, and removes correctly answered words from the remaining session set. These files are evidence only and remain outside implementation scope.
- `context.md` correctly records static hosting, no toolchain, the two vocabulary tools, the shared stylesheet, and auxiliary products. It does not yet distinguish the four-page public career presentation from legacy utilities and currently gives the China gallery equal prominence, while the request forbids promoting it from the redesigned pages.
- Asset inspection confirms two portrait files and all 13 referenced artwork originals. The live portrait is the smaller gray-jacket image; the alternative black-jacket portrait is substantially larger and currently unused. Artwork source dimensions vary widely, making natural-ratio layout mandatory.
- No attachments or relevant context extensions were present, and `.aib_memory/instructions.md` was empty.

### Industry Knowledge and Applicability

- The W3C WCAG 2.2 Recommendation defines Level AA reflow as content remaining available without two-dimensional scrolling at a width equivalent to 320 CSS pixels. W3C techniques also identify flexible layout, fitted images, `prefers-reduced-motion`, strong focus indication, and adequately sized targets as relevant techniques. Applicability: build single-column fallbacks, avoid fixed image/menu sizes, ensure the sticky header cannot obscure focus, and test keyboard, zoom/reflow, text spacing, contrast, and reduced-motion behavior. Sources: [WCAG 2.2](https://www.w3.org/TR/WCAG22/) and [WCAG 2.2 Techniques](https://www.w3.org/WAI/WCAG22/Techniques/).
- Browser image guidance recommends intrinsic `width` and `height` attributes to reserve layout space, native lazy loading only for off-screen images, and eager loading for likely largest-contentful-paint imagery. Applicability: render the portrait eagerly with its 600 × 800 dimensions and give all below-fold artwork its source dimensions plus `loading="lazy"`; do not add a JavaScript lazy-loader. Source: [web.dev browser-level image lazy loading](https://web.dev/articles/browser-level-image-lazy-loading).
- Google Search Central recommends self-referential canonical links in valid HTML heads and absolute canonical URLs, with consistent internal linking. Applicability: place `https://hmhristov.com/` or the exact `.html` canonical in each static page head and keep ordinary HTML navigation links aligned with those public filenames. Source: [Google canonical URL guidance](https://developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls).
- The Open Graph protocol requires `og:title`, `og:type`, `og:image`, and `og:url` for a complete graph object and recommends a concise description and image alternative. Applicability: provide page-specific `website` metadata with absolute local-image URLs and matching accessible image descriptions; the exact image strategy is an open decision. Source: [Open Graph protocol](https://ogp.me/).
- The widely available `prefers-reduced-motion` media feature reflects an operating-system request to reduce non-essential animation. Applicability: reveal content must be visible by default, JavaScript may opt in to motion only after capability detection, and the reduced-motion branch should remove transforms and non-essential transitions. Source: [MDN prefers-reduced-motion](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/%40media/prefers-reduced-motion).

### AI Agent critique

- Request quality: the supplied scope and acceptance list are unusually complete, and the eight follow-up answers now resolve résumé length, professional-fact handling, navigation enhancement, GitHub visibility, palette, motion, gallery caption policy, and social imagery.
- Request factual risk: “currently,” “Present,” leadership-role wording, skill levels, and certification listings originate in old page copy; the developer explicitly chose to preserve published status, so the implementation must retain rather than strengthen those claims and the residual staleness risk remains accepted.
- Request testability: several acceptance phrases—“coherent,” “equally intentional,” “professional,” “meaningful,” and “materially improves”—remain partly qualitative, but the selected palette, layout, motion, contact, caption, résumé, and social-preview alternatives provide observable implementation boundaries alongside the 35 directly testable criteria.
- Context drift: `context.md` describes the whole product but not the desired primary career-site hierarchy, resilient four-page navigation, editorial design rules, or main-page accessibility/security baseline, so those stable decisions will need to be added after implementation.
- Context scope tension: the China gallery remains a legitimate product feature in context, while this request bans its promotion from the four redesigned pages; the correct resolution is to preserve the feature and remove only main-site discovery paths.
- Shared-CSS regression risk: `context.md` correctly says `css/main.css` is shared by all root-level pages, but the request names only four HTML consumers; unscoped replacement styles would unintentionally redesign three excluded pages.
- `index.html`: invalid body and paragraph structure, absent portrait alternative/dimensions, raw non-linked email, no semantics or page-specific metadata, third-person voice, and generic contacts weaken accessibility, validity, privacy clarity, and requested positioning.
- `cv.html`: obsolete browser boilerplate, malformed conditional comment structure, missing list closure, empty script source, nested presentation tables, overlong chronology, stale-currentness risk, and no print treatment conflict with the requested result.
- `apps.html`: missing `h1`, absent supported project descriptions, duplicated menu markup, weak heading hierarchy, and generic metadata prevent it from functioning as a professional Projects page.
- `art_drawing.html`: missing `h1`, misspelled medium, distorted fixed-height rendering, inaccurate and duplicate alternatives, no figures or grouping landmarks, no lazy loading or dimensions, and invalid body closure undermine both curatorial quality and accessibility.
- `css/main.css`: selector leakage, fixed typography and image height, possible horizontal overflow, hover-dependent affordance, sticky-header focus risk, and absent focus-visible, reduced-motion, print, and responsive states make incremental patching less reliable than a scoped design-system rewrite with retained legacy fallback rules.
- Vocabulary-app evidence: the app files provide enough supported behavior for concise project descriptions, but their own inline styles, inline event assignment, debug logging, and `innerHTML` fallback are out of scope and must not be “cleaned up” opportunistically.
- Asset quality: the live portrait is efficient enough for normal display; the unused alternate portrait is over 3 MB. The small artwork originals do not need derivatives for basic delivery, but their varying ratios expose the current gallery distortion.
- Analysis workflow quality: `aib-analyze.md` duplicates three variable definitions, states that Proposed Solution must contain exactly three `###` subsections while naming only two, skips S05.7, and says input reset is deferred when questions exist while sequencing unconditional finalization before question generation. The named conventions and executable sequence are followed here, but these internal inconsistencies should be corrected separately.
- Convention interaction: `analysis-convention.md` names two Proposed Solution subsections, while the parent prompt says three; `q-block-convention.md` requires a recommended choice even though the analysis Decision Register requires neutral presentation for `ask` decisions. Recommendations will therefore appear only in generated Q-blocks, not in the Decision Register.
- Requirements convention: the priority/urgency gate item is not naturally relevant to every already-authorized implementation request; for this run, execution of the active request is treated as sufficient priority rather than creating a product-design question.
- Plan convention: its requirement to embed exact `edit-context.py` commands is useful for deterministic handoff; the generated plan must include literal atomic context mutations and must not require the implement agent to read `context.md`.
- Plan-schema conflict: `aib-analyze.md` states in its objective that the plan is updated with `## Plan` and `## Decisions`, but `plan-convention.md` permits exactly Goal, Constraints, Success criteria, and Plan. The normative plan schema is preserved, with resolved decisions recorded under Constraints instead of adding a prohibited fifth section.
- Auto-request prompt: the derived title accurately reflects the first goal sentence and fits the 60-character limit; no quality defect affected request creation.

### Edge Cases

- The site may be served as `/` or `/index.html`; active navigation logic must treat both as Home.
- JavaScript may fail, be blocked, load late, or lack Intersection Observer; hard-coded navigation and visible content must remain complete.
- Reduced-motion preference may change or be enabled before load; content cannot start hidden outside an enhancement-only class and transforms must be removed in the reduced branch.
- A 320 CSS-pixel viewport, 400% zoom, long translated project names, long URLs, or user-defined text spacing can cause wrapping and header obstruction if widths or nowrap rules persist.
- Keyboard focus can move behind a sticky header; skip-link and anchor targets need adequate scroll offset or a non-sticky small-screen header.
- CSS changes can leak into `cl.html`, `links_backup.html`, or `html_template.html`; all redesigned component rules need body-level scoping or explicitly preserved legacy selectors.
- The homepage portrait is above the fold and must not be lazy-loaded; gallery images below the first viewport should be lazy-loaded without zero-dimension layout collapse.
- Gallery images include portrait, landscape, square, and near-square ratios; a fixed row or forced crop would violate preservation requirements.
- Two existing artwork labels are factually wrong and several are generic; visible captions must not turn visual inference into unsupported title or provenance claims.
- If an art image fails, the alternative must still distinguish the work without claiming an official title.
- Print output must not include sticky navigation, reveal transforms, buttons styled as controls, decorative surfaces, or clipped multi-column sections; link destinations should remain intelligible where useful.
- External links may remain in the same context or open a new one; only new-context links require `noopener noreferrer`, and visual wording should not falsely imply a download or form submission.
- Social crawlers may not execute JavaScript and require absolute metadata in source HTML; the selected local preview image must be publicly addressable and accurately described.
- The existing email must remain a functioning `mailto:` action, while GitHub must stay tertiary and must not compete with LinkedIn or email in the contact hierarchy.
- Current employment and certification claims may have changed since the legacy copy was written; the selected preserve-published-status approach accepts that risk but does not permit strengthening or independently recharacterizing those assertions.
- The four selected social-preview derivatives need stable public paths, accurate alternative metadata, predictable crops or padding, and optimization without replacing or modifying the original portrait and artwork files.

### Requirements Gate Evaluation

Requirements Gate: 8/8 PASS — all categories satisfied.

## Proposed Solution

### High-Level Concept

Rebuild the four public pages as a semantic, resilient career-site family using scoped design tokens and reusable HTML patterns, with a small enhancement-only script for shared navigation state and motion. Preserve facts and assets, sharply condense professional material, present only the two supported tools and all 13 artworks, and make metadata, print, keyboard, mobile, reduced-motion, no-script, and image behavior first-class.

### Execution Steps

#### Task 1: Establish the shared presentation system

- `css/main.css`: replace the four-page presentation with scoped cream, charcoal, deep-rust, and muted-sage tokens; responsive layout utilities; components; natural-ratio media; strong focus states; 320-pixel reflow; subtle opacity-and-short-rise reveals; reduced-motion overrides; and one-page Professional print rules while retaining compatibility for excluded legacy consumers.
  - Cross-file invariant: all redesigned selectors must require a body-level site class so `cl.html`, `links_backup.html`, and `html_template.html` are not unintentionally redesigned.
- `js/site.js`: add small dependency-free in-place enhancement for active navigation state and one-time reveal behavior without replacing markup, generating essential content, or using unsafe HTML insertion.

#### Task 2: Rebuild the editorial homepage

- `index.html`: create valid first-person landmark-based content with resilient Home/Professional/Projects/Creative navigation, preserved published professional status, professional hero, current portrait, primary LinkedIn and secondary email actions, a tertiary GitHub footer/profile link, three equal pillar previews, page-specific metadata referencing `images/social/home-preview.jpg`, intrinsic image dimensions, and scoped shared-script hooks.

#### Task 3: Condense the professional profile

- `cv.html`: replace the full layout-table chronology with a factual third-person one-page executive profile containing preserved published status, summary, capability evidence, education, skills, languages, certifications, primary LinkedIn and secondary email actions, a tertiary GitHub link, semantic structure, metadata referencing `images/social/professional-preview.jpg`, and print-ready classes.

#### Task 4: Present the two supported tools

- `apps.html`: create a semantic Projects page with exactly two consistent cards, workspace-supported purpose and learning-use copy, direct ordinary links to the unchanged applications, shared navigation and tertiary-GitHub footer patterns, and unique metadata referencing `images/social/projects-preview.jpg`.

#### Task 5: Curate the complete artwork gallery

- `art_drawing.html`: create a first-person Creative page with all ten oil works and three watercolors in a natural-ratio card grid, concise evidence-supported captions, corrected medium naming and alternatives, intrinsic dimensions, appropriate below-fold lazy loading, resilient shared components, a tertiary-GitHub footer pattern, and unique metadata referencing `images/social/creative-preview.jpg`.

#### Task 6: Create optimized social-preview derivatives

- `images/social/home-preview.jpg`: create an optimized 1200 × 630 Home sharing composition derived from the current portrait without modifying the original.
- `images/social/professional-preview.jpg`: create an optimized 1200 × 630 Professional sharing composition derived from the current portrait without modifying the original.
- `images/social/projects-preview.jpg`: create an optimized 1200 × 630 Projects sharing composition derived from the current portrait without modifying the original.
- `images/social/creative-preview.jpg`: create an optimized 1200 × 630 Creative sharing composition derived from a representative existing artwork without modifying the original.

#### Task 7: Verify behavior and boundaries

- `python3 -m http.server 8765`: serve the repository locally and confirm all four public routes and unchanged vocabulary-application routes return successful responses.
- `google-chrome --headless=new`: render each page at desktop, 320-pixel mobile, no-script, reduced-motion, and print conditions and capture evidence that navigation, content, image ratios, metadata, focus order, and print visibility meet acceptance expectations.
- `rg`: verify exact navigation labels and links, unique titles/descriptions/canonicals/Open Graph fields, thirteen artwork references, two project links, no China-gallery link, no third-party script/font, no inline handler, safe external-link relations, and no modified paths below `202507_china/`.

#### Task 8: Update durable product context

- `.aib_memory/context.md`: record the four-page career-site hierarchy, resilient shared navigation, scoped CSS compatibility boundary, accessibility/security/metadata rules, factual content boundary, and resolved conflict treatment; update affected Product, Concepts, Requirements, Solution, File Structure, and Issues entries only through the approved context editor commands in the generated implementation plan.

## Context Update Analysis

- Context Elements to Add:
  - Product: identify Home, Professional, Projects, and Creative as the primary career presentation for professional visitors while leaving auxiliary tools deployed but secondary.
  - Concepts: record hard-coded semantic navigation enhanced in place by `js/site.js`, with essential content and links independent of JavaScript and no navigation-node replacement.
  - Concepts: record the `.site-page`-style CSS scoping boundary required because `css/main.css` also serves excluded legacy pages.
  - Requirements: require exact visible navigation labels and mappings, a no-JavaScript fallback, static source metadata, current-page indication, 320-pixel reflow, visible focus, reduced motion, natural-ratio images with dimensions, safe external links, and factual-copy discipline.
  - Solution: describe the rust-and-muted-sage four-page component system, one-page executive résumé/print model, exactly two tool cards, natural-ratio captioned two-medium art gallery, four optimized page-specific social previews, tertiary GitHub placement, subtle reveal treatment, and dependency-free in-place enhancement script.
  - Issues: add the legacy-page validity, inaccurate artwork alternatives, shared-CSS leakage risk, time-sensitive professional fact risk, and metadata/accessibility gaps identified in the critique; preserve unrelated existing issue entries.
- Context Elements to Modify/Remove:
  - Refine Product's current “Site sections” statement so it distinguishes the four primary career pages from auxiliary tools and the separate China gallery rather than implying equal main-navigation priority.
  - Refine Product's broad “Primary audience is the owner and public visitors” statement to include professional visitors as the primary audience of the four redesigned pages without removing general public use.
  - Extend, rather than delete, the Concepts statement that `css/main.css` is shared by root pages so it also records the selector-isolation requirement.
  - Update the four affected File Structure descriptions after implementation; do not delete unrelated file entries.
- Conflict Resolution & Intent Preservation:
  - The context's China-gallery product entry conflicts only with promotion from the redesigned pages, not with existence of the gallery. Preserve the gallery architecture and all `202507_china/` facts unchanged while defining it as an auxiliary, unlinked feature in the four-page career presentation.
  - The shared-stylesheet concept conflicts with a four-page-only visual rewrite because three excluded root pages import the same file. Preserve the original intent of one shared stylesheet by retaining legacy compatibility and requiring all new design-system selectors to be scoped by an explicit body class used only on the four redesigned pages.
  - The static plain-HTML architecture aligns with the request. Preserve it by limiting the script to enhancement, keeping source navigation and metadata in each HTML file, and adding no toolchain or dependency.
  - Gallery-specific `innerHTML` and external `marked.js` context requirements apply to excluded products. Preserve those requirements untouched and do not generalize them into the redesigned pages, which should add no injection surface or third-party script.
  - The unchanged-originals requirement could conflict with optimized social previews if derivatives replaced or overwrote source images. Preserve the originals and create the four previews only at new stable paths under `images/social/`.
  - The factual-copy discipline could conflict with preserving legacy “Present” and current-status language. Preserve the developer-selected published wording without adding stronger recency, verification, or credential claims, and record the residual staleness risk as an accepted implementation constraint.

## Decision Register

### Decision Points

#### Decision Point: Professional fact currency

- Tag: resolved-by-user
- Applies to: Task 2 homepage copy and Task 3 professional profile; alternatives exist because the legacy pages use present-tense employment, “Present” dates, skill levels, and certification labels that may have changed, while the request prohibits invented currentness.
- Chosen alternative — Preserve published status: retain the current-role and date claims as written, editing only grammar and avoiding any new verification claim; benefit is maximum continuity, drawback is possible stale information, and impact is current-tense homepage and Professional copy.
- Rationale/Resolution: In Q001, the developer selected “Preserve published status.”

#### Decision Point: Professional-page editorial structure

- Tag: resolved-by-user
- Applies to: Task 3 Professional-page content architecture and print layout; alternatives exist because “concise résumé” permits several materially different evidence structures.
- Chosen alternative — One-page executive profile: omit role-by-role chronology and present summary, capability evidence, education, skills, languages, and credentials on one printed page; benefit is brevity, drawback is limited career context, and impact is the most aggressive content reduction and print constraints.
- Rationale/Resolution: In Q002, the developer selected “One-page executive profile.”

#### Decision Point: Shared navigation enhancement model

- Tag: resolved-by-user
- Applies to: Task 1 `js/site.js` and the header markup in Tasks 2–5; alternatives exist because the input explicitly permits JavaScript to “replace, synchronize, or enhance” the hard-coded navigation.
- Chosen alternative — Enhance in place: keep complete HTML navigation authoritative and let JavaScript only synchronize active state and enhancement hooks; benefit is strongest resilience and simplest script, drawback is duplicated link markup, and impact is identical nav HTML in four pages plus a small state script.
- Rationale/Resolution: In Q003, the developer delegated selection of the most appropriate approach. Enhance in place is selected because it most directly satisfies the hard-coded fallback requirement while minimizing DOM replacement, focus, and synchronization failure modes.

#### Decision Point: GitHub profile placement

- Tag: resolved-by-user
- Applies to: contact and footer content across Tasks 2–4; alternatives exist because the current homepage exposes GitHub, while the new input names only LinkedIn as primary and email as secondary and limits Projects to two application cards.
- Chosen alternative — Keep GitHub as a tertiary footer/profile link: preserve it outside primary contact actions and project-card counts; benefit is continued technical evidence without competing with contact priorities, drawback is an additional repeated external link, and impact is shared footer or Professional profile markup.
- Rationale/Resolution: In Q004, the developer selected “Keep GitHub as a tertiary footer/profile link.”

#### Decision Point: Visual palette

- Tag: resolved-by-user
- Applies to: Task 1 design tokens and every redesigned page; alternatives exist because the input bounds the style as warm, earthy, restrained, and editorial but does not choose exact hues.
- Chosen alternative — Rust and muted sage: cream paper surfaces, charcoal text, a deep rust primary accent, and subdued sage secondary surface; benefit is warm editorial character with distinct secondary cues, drawback is a more crafted visual personality, and impact is the complete token palette and component states.
- Rationale/Resolution: In Q005, the developer selected “Rust and muted sage.”

#### Decision Point: Motion treatment

- Tag: resolved-by-user
- Applies to: Task 1 reveal system and the section/card hooks in Tasks 2–5; alternatives exist because visible controlled entrance and scroll motion can differ significantly in visual emphasis while meeting progressive-enhancement and reduced-motion constraints.
- Chosen alternative — Subtle opacity and short rise: reveal once with a small vertical translation and restrained stagger; benefit is clear hierarchy with low distraction, drawback is still spatial motion, and impact is Intersection Observer classes plus reduced-motion removal.
- Rationale/Resolution: In Q006, the developer selected “Subtle opacity and short rise.”

#### Decision Point: Creative-gallery presentation

- Tag: resolved-by-user
- Applies to: Task 5 gallery markup, captions, and layout; alternatives exist because all works must remain complete and uncropped, but grid rhythm and visible caption density remain open.
- Chosen alternative — Natural-ratio card grid with concise captions: use a responsive grid of figures whose images retain full ratios and whose captions reuse only corrected supported descriptions; benefit is clear scanability and explicit context, drawback is more visible text, and impact is one figure/caption per artwork.
- Rationale/Resolution: In Q007, the developer selected “Natural-ratio card grid with concise captions.”

#### Decision Point: Social-preview image strategy

- Tag: resolved-by-user
- Applies to: metadata in Tasks 2–5 and any optional derivative asset; alternatives exist because each page needs appropriate social presentation, but the repository contains no precomposed wide preview assets.
- Chosen alternative — Optimized page-specific derivatives: create stable cropped or padded social variants from the portrait and representative artwork without changing originals; benefit is predictable rich-preview composition, drawback is additional generated assets and maintenance, and impact is a small `images/social/` set plus metadata.
- Rationale/Resolution: In Q008, the developer selected “Optimized page-specific derivatives.”
