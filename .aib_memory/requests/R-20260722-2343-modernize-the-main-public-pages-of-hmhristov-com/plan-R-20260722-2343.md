## Goal

Modernize `index.html`, `cv.html`, `apps.html`, and `art_drawing.html` into a coherent, static career-site presentation that helps professional visitors understand Hristo M. Hristov's leadership profile, two vocabulary projects, artwork, and contact paths. The change impacts the four public HTML pages, the shared `css/main.css`, a new `js/site.js`, four new social-preview assets under `images/social/`, automated verification, and durable workspace context.

## Constraints

- Preserve the public filenames and GitHub Pages deployment model; use only plain HTML, CSS, vanilla JavaScript, and static assets.
- Do not modify files below `202507_china/`, either vocabulary application, either original portrait, or any original artwork.
- Do not add a framework, build system, backend, analytics, form service, external font, third-party script, inline event handler, secret, or untrusted HTML insertion.
- Scope every new presentation rule in `css/main.css` to `body.site-page` and retain legacy behavior for `cl.html`, `links_backup.html`, and `html_template.html`.
- Keep semantic navigation and all essential content in each HTML source; `js/site.js` may enhance the existing nodes in place but must not replace them or become necessary for access.
- Preserve the professional status, dates, skill levels, and credentials published in the legacy pages; edit grammar and structure without strengthening their currency or implying independent verification.
- Use a one-page executive-profile model for `cv.html` rather than a chronology-led résumé.
- Keep LinkedIn primary, email secondary, and GitHub tertiary in the contact hierarchy.
- Use cream paper surfaces, charcoal text, deep-rust primary accents, and muted-sage secondary surfaces.
- Use one-time subtle opacity-and-short-rise reveals, with content visible on failure and all non-essential motion removed for reduced-motion users.
- Present all artwork in a natural-ratio card grid with concise, evidence-supported captions.
- Create four optimized, page-specific 1200 × 630 social-preview derivatives without altering source images.
- Treat the 35 acceptance criteria captured in the request as binding; do not promote or link the China gallery from the redesigned pages.

## Success criteria

- All four pages expose visible Home, Professional, Projects, and Creative links to `index.html`, `cv.html`, `apps.html`, and `art_drawing.html`; the current page is indicated in source markup and all navigation remains usable with JavaScript disabled.
- `index.html` opens with Hristo's name, a first-person senior data-and-analytics proposition, the current portrait, primary LinkedIn and secondary email actions, and routes to three equally developed pillar previews.
- `cv.html` renders and prints as a concise one-page third-person executive profile while retaining supported leadership, capability, education, skill, language, certification, and selected experience evidence.
- `apps.html` contains exactly two project cards and direct links to the unchanged `apps/PUK_English_Words.html` and `apps/hhwords/index.html` applications.
- `art_drawing.html` contains all 13 existing artwork references, grouped as ten oil works and three watercolors, with no forced crop or ratio distortion and with corrected, distinguishing alternatives and concise captions.
- All four pages remain readable without horizontal page scrolling at a 320 CSS-pixel viewport, retain visible keyboard focus, use practical target sizes, and keep content available under no-script and reduced-motion conditions.
- Each page has a unique title, description, canonical URL, Open Graph title, description, URL, type, image, and image alternative in source HTML; each references its own existing 1200 × 630 optimized social-preview file.
- The principal portrait loads eagerly with intrinsic dimensions; below-fold artwork uses intrinsic dimensions and native lazy loading without layout collapse.
- External links that open a new browsing context use safe relationship attributes; there is no contact form, new third-party request, unsafe protocol, inline handler, secret, or new injection surface.
- `cl.html`, `links_backup.html`, and `html_template.html` retain their legacy presentation, and the repository diff contains no changes below `202507_china/` or inside either vocabulary application.
- Desktop, 320-pixel mobile, keyboard, no-JavaScript, reduced-motion, and print checks pass in current Chromium; route and source assertions report the expected counts and no errors.
- `.aib_memory/context.md` passes its verifier after the implemented architecture, requirements, solution, file structure, and residual issues are recorded.

## Plan

### Task 1: Establish the shared presentation system

#### Intent

Create the scoped visual system and non-essential behavior used by the four redesigned pages while preserving excluded consumers of the shared stylesheet.

#### Procedure

- `css/main.css`: retain the legacy rules needed by excluded root pages and add a `body.site-page`-scoped warm editorial token system, responsive layout and component rules, natural-ratio media behavior, visible focus treatment, 320-pixel reflow, subtle opacity-and-short-rise reveals, reduced-motion overrides, and a one-page Professional print layout.

- `js/site.js`: create a small dependency-free script that enhances the hard-coded navigation in place, treats `/` and `/index.html` as Home, preserves the source-level current-page state, enables one-time reveal classes only after capability detection, and leaves all content visible when JavaScript or Intersection Observer is unavailable.

#### Done criteria

- New design selectors require `body.site-page`; the script neither generates navigation nor inserts untrusted HTML.

### Task 2: Rebuild the editorial homepage

#### Intent

Turn `index.html` into the first-person professional gateway for the three supporting pillars and contact paths.

#### Procedure

- `index.html`: replace the malformed legacy document with valid English-language header, navigation, main, section, and footer landmarks; apply `site-page`; add a skip link and source-level Home state; retain the current `images/HHristov_20240407_110742.jpg` portrait with accurate alternative text, 600 × 800 intrinsic dimensions, and eager loading; preserve the published current-role and credential status in a concise first-person leadership proposition; make LinkedIn primary, the existing `mailto:` address secondary, and GitHub tertiary; add equally developed Professional, Projects, and Creative previews; load `js/site.js` deferentially; and add unique canonical and social metadata referencing `https://hmhristov.com/images/social/home-preview.jpg`.

#### Done criteria

- The first viewport establishes identity, proposition, portrait, and contact hierarchy, and all three pillar routes remain ordinary links without JavaScript.

### Task 3: Condense the professional profile

#### Intent

Replace `cv.html` with a factual one-page executive profile that works on screen and in print.

#### Procedure

- `cv.html`: remove obsolete conditional markup, empty script loading, nested presentation tables, and the role-by-role chronology; create valid English-language landmark markup with `site-page`, skip link, complete navigation and source-level Professional state; preserve the published current role and dates; organize supported facts into executive summary, leadership and delivery evidence, data and architecture capability, organizational development, hands-on depth, education, skills, languages, and certifications; keep LinkedIn primary, email secondary, and GitHub tertiary; load `js/site.js`; add print-oriented classes; and add unique canonical and social metadata referencing `https://hmhristov.com/images/social/professional-preview.jpg`.

#### Done criteria

- The page contains no layout tables or full chronology and produces one readable printed page without navigation, reveal transforms, clipped sections, or decorative control styling.

### Task 4: Present the two supported projects

#### Intent

Make `apps.html` a credible Projects page without changing either application.

#### Procedure

- `apps.html`: rebuild the page with valid English-language landmarks, `site-page`, skip link, complete navigation and source-level Projects state; include exactly two consistent project cards whose descriptions are limited to the workspace-supported bidirectional inline-data PUK quiz and dataset-backed HH Words quiz behavior; retain direct ordinary links to `apps/PUK_English_Words.html` and `apps/hhwords/index.html`; add the shared tertiary-GitHub footer, deferred `js/site.js`, and unique canonical and social metadata referencing `https://hmhristov.com/images/social/projects-preview.jpg`.

#### Done criteria

- Exactly two cards and two launch destinations are present, and neither application file is changed.

### Task 5: Curate the complete artwork gallery

#### Intent

Make `art_drawing.html` a complete, responsive, first-person Creative page with accurate grouping and descriptions.

#### Procedure

- `art_drawing.html`: rebuild the malformed document with valid English-language landmarks, `site-page`, skip link, complete navigation and source-level Creative state; present `art/201501_IMG_6502_23.JPG`, `art/201503_IMG_6503_23.JPG`, `art/201505_DSC06570_23.JPG`, `art/201506_IMG_7038_23.JPG`, `art/201506_IMG_7088_23.JPG`, `art/201506_IMG_7093_23.JPG`, `art/201507_IMG_7084_23.JPG`, `art/201507_IMG_7101_23.JPG`, `art/201508_IMG_7202_23.JPG`, and `art/201602_IMG_9755_23.JPG` as ten oil figures and `art/201507_IMG_7149_23.JPG`, `art/201508_IMG_7480_23.JPG`, and `art/201604_DSC01268_23.JPG` as three watercolor figures; use their natural ratios, intrinsic dimensions, and below-fold native lazy loading; provide concise evidence-supported captions and distinguishing alternatives, correcting the portrait mislabeled Bezbog, the running horses mislabeled Boat, untranslated Makove wording, and Watercollor spelling; add the shared tertiary-GitHub footer, deferred `js/site.js`, and unique canonical and social metadata referencing `https://hmhristov.com/images/social/creative-preview.jpg`.

#### Done criteria

- The page contains 13 figures in the required 10/3 grouping, and every complete original remains uncropped and unmodified.

### Task 6: Create optimized social-preview derivatives

#### Intent

Produce stable page-specific sharing assets while preserving every original image.

#### Procedure

- `images/social/home-preview.jpg`: create an optimized 1200 × 630 Home composition derived from `images/HHristov_20240407_110742.jpg`, using the selected rust-and-sage system and sufficient safe space for social cropping.

- `images/social/professional-preview.jpg`: create a distinct optimized 1200 × 630 Professional composition derived from `images/HHristov_20240407_110742.jpg`, using the selected system and professional-page identity.

- `images/social/projects-preview.jpg`: create a distinct optimized 1200 × 630 Projects composition derived from `images/HHristov_20240407_110742.jpg`, using the selected system and project-page identity without implying additional applications.

- `images/social/creative-preview.jpg`: create an optimized 1200 × 630 Creative composition derived from `art/201501_IMG_6502_23.JPG`, using crop or padding only in the derivative and leaving the complete original unchanged.

#### Done criteria

- Four valid 1200 × 630 JPEG files exist at the named paths, remain legible in common social crops, and do not replace or alter their sources.

### Task 7: Run automated and browser verification

#### Intent

Verify every testable success criterion and collect local evidence for responsive, resilient, accessible, metadata, print, and scope-boundary behavior.

#### Procedure

- `python3 -m http.server 8765`: run this command from the repository root; expect a local server at `http://127.0.0.1:8765/` and successful responses for `index.html`, `cv.html`, `apps.html`, `art_drawing.html`, `apps/PUK_English_Words.html`, and `apps/hhwords/index.html`.

- `rg`: run repository searches across `index.html`, `cv.html`, `apps.html`, and `art_drawing.html`; expect four exact navigation labels and mappings per page, one current-page marker per page, unique titles/descriptions/canonicals/Open Graph fields, four page-specific social-image paths, two project destinations, 13 artwork references, no China-gallery link, no external font or third-party script, no inline handler, and safe relationships on every external link that opens a new context.

- `google-chrome --headless=new`: render each of `index.html`, `cv.html`, `apps.html`, and `art_drawing.html` from the local server at desktop and 320 × 900 viewports; expect screenshots under `/tmp/` with no horizontal page overflow, clipped navigation, distorted media, or hidden content.

- `google-chrome --headless=new --disable-javascript`: render each of the four local routes; expect complete navigation, contact paths, projects, professional content, and artwork in the dumped DOM or screenshots under `/tmp/`.

- `google-chrome --headless=new --force-prefers-reduced-motion`: render each of the four local routes; expect screenshots under `/tmp/` with all content visible and no reveal transform required for access.

- `google-chrome --headless=new --print-to-pdf=/tmp/cv-print.pdf --no-pdf-header-footer http://127.0.0.1:8765/cv.html`: generate the Professional print artifact; expect `/tmp/cv-print.pdf` to contain exactly one readable page with navigation and non-content decoration omitted.

- `git diff --name-only -- 202507_china apps/PUK_English_Words.html apps/hhwords/index.html images/HHristov_20240405.jpg images/HHristov_20240407_110742.jpg art`: run this scope-boundary check; expect no output for excluded applications, original portraits, original artwork, or China-gallery files.

- `index.html`, `cv.html`, `apps.html`, and `art_drawing.html`: inspect the rendered pages with keyboard-only navigation in a current Chromium browser; expect a logical focus order, visible focus, functional skip links, unambiguous link purpose, no hover-only content, and no focus obscured by the header.

#### Done criteria

- All automated counts, route checks, screenshots, print output, keyboard checks, and scope-boundary checks match the expected outcomes, with any defect fixed and the affected checks rerun.

### Task 8: Update durable product context

#### Intent

Record the implemented career-site architecture, resolved decisions, compatibility boundaries, file inventory, and residual risks without disturbing unrelated product context.

#### Procedure

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Product --text "Primary audience is the owner and public visitors."`; expect exit code 0 and removal of exactly that legacy audience statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Product --text "Primary audience for the four-page career presentation is professional visitors; the owner and general public remain audiences for the broader personal site."`; expect exit code 0 and one new Product statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Product --text "Site sections: personal homepage, CV, mini web apps, art gallery, China travel photo gallery, checklist page, GitHub markdown reader."`; expect exit code 0 and removal of exactly that undifferentiated site-sections statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Product --text "Primary career-site pages are Home (index.html), Professional (cv.html), Projects (apps.html), and Creative (art_drawing.html); the China gallery, checklist, Markdown reader, and other utilities remain deployed as auxiliary features outside this primary navigation."`; expect exit code 0 and one new Product hierarchy statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation delete --area Concepts --text "Site-wide shared stylesheet at css/main.css used by all root-level pages."`; expect exit code 0 and removal of exactly that incomplete stylesheet statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Concepts --text "css/main.css remains shared by root-level pages, while the modern four-page design system is scoped to body.site-page so excluded legacy consumers retain their prior presentation."`; expect exit code 0 and one updated stylesheet-boundary statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Concepts --text "The four primary pages contain complete semantic navigation in source HTML; js/site.js enhances those nodes in place for active-location and reveal behavior, and essential content remains independent of JavaScript."`; expect exit code 0 and one navigation-enhancement statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Requirements --type MUST --text "The four primary pages must expose visible Home, Professional, Projects, and Creative navigation mapped to index.html, cv.html, apps.html, and art_drawing.html, with a source-level current-page indication and functional no-JavaScript fallback."`; expect exit code 0 and one MUST requirement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Requirements --type MUST --text "The four primary pages must reflow at 320 CSS pixels, provide visible keyboard focus and practical target sizes, preserve content under reduced motion or script failure, and use natural-ratio images with intrinsic dimensions."`; expect exit code 0 and one MUST requirement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Requirements --type MUST --text "Each primary page must include unique source-level title, description, canonical URL, Open Graph metadata, and accurate page-specific social-preview image metadata."`; expect exit code 0 and one MUST requirement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Requirements --type MUST --text "Professional copy must use only facts published in the repository, preserve the developer-selected legacy status wording without stronger currency claims, and keep LinkedIn primary, email secondary, and GitHub tertiary."`; expect exit code 0 and one MUST requirement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Requirements --type "MUST NOT" --text "Original portrait and artwork files must not be modified or replaced when optimized derivatives are created."`; expect exit code 0 and one MUST NOT requirement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "The four-page career site uses a scoped cream, charcoal, deep-rust, and muted-sage editorial component system with responsive, focus, reduced-motion, natural-image, and print treatments."`; expect exit code 0 and one Solution statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "The Professional page is a one-page executive profile; the Projects page contains exactly the two vocabulary tools; and the Creative page contains all 13 works in a natural-ratio captioned grid grouped as ten oils and three watercolors."`; expect exit code 0 and one Solution statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "js/site.js provides dependency-free in-place navigation and subtle opacity-and-short-rise reveal enhancement without generating essential markup."`; expect exit code 0 and one Solution statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Solution --text "Four optimized 1200 by 630 page-specific sharing images live under images/social and are derived without modifying the original portrait or artwork."`; expect exit code 0 and one Solution statement.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "Published current-role, Present-date, skill-level, and certification wording is intentionally preserved and may become stale without periodic owner review."`; expect exit code 0 and one residual factual-risk issue.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "The vocabulary applications retain out-of-scope inline styling, inline event assignment, debug logging, and innerHTML fallbacks identified during the main-page redesign."`; expect exit code 0 and one out-of-scope application issue.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "aib-analyze.md duplicates question variables, declares an impossible three-subsection Proposed Solution count while naming two, skips S05.7, and conflicts with itself about input-reset timing."`; expect exit code 0 and one AIB workflow-quality issue.

- `.aib_memory/context.md`: run `python .aib_brain/tools/edit-context.py --workspace . --operation insert --area Issues --text "AIB conventions conflict over a prohibited plan Decisions section and over neutral Decision Register choices versus recommended Q-block choices; the requirements gate priority item also fits already-authorized requests poorly."`; expect exit code 0 and one AIB convention-interaction issue.

- `.aib_memory/context.md`: replace the exact File Structure lines `index.html - personal homepage with profile summary, interests, and contacts`, `cv.html - curriculum vitae page`, `apps.html - listing page linking to mini web apps`, `art_drawing.html - art/drawing gallery page`, `css/main.css - shared site-wide stylesheet for root-level pages`, and `images/ - 2 profile photo files` with implemented descriptions of the editorial Home page, one-page Professional profile, two-card Projects page, 13-work Creative gallery, legacy-compatible scoped shared stylesheet, and original portraits plus `images/social/` derivatives; add `js/site.js - dependency-free in-place navigation-state and reveal enhancement script`. Preserve every unrelated File Structure entry exactly.

- `.aib_memory/context.md`: run `python .aib_brain/tools/verify-context.py --workspace .`; expect all 12 checks to pass and the verification result to be recorded as valid.

#### Done criteria

- Every command exits successfully, the exact legacy statements are replaced rather than duplicated, unrelated context and issues remain intact, File Structure matches the implemented files, and context verification reports 12/12 passing checks.
